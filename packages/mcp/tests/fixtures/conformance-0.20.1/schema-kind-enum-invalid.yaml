kind: widget
# rejectedBy: schema
id: bad-kind
name: Bad Kind
description: An entry whose kind is neither a well-known value nor a namespaced custom one (no dot, so it doesn't match the namespaced-kind pattern either).
