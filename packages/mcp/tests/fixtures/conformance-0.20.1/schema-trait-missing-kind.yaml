kind: component
# rejectedBy: schema
id: bad-trait-missing-kind
name: Bad Trait Missing Kind
description: "A component trait with neither `kind: boolean` nor `kind: enum` - the tag that says which of the two shapes applies."
traits:
  - id: loading
    description: Shows a spinner in place of the label.
