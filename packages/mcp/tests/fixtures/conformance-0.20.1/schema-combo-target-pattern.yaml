kind: component
# rejectedBy: schema
# errorAt: /combos/0/items/0
id: bad-combo-target
name: Bad Combo Target
description: A component combo item that matches neither a bare id nor the braces-wrapped token reference pattern.
traits:
  - kind: boolean
    id: loading
    description: Shows a spinner in place of the label.
combos:
  - subject: loading
    level: must-not
    items: ["{Bad Format}"]
