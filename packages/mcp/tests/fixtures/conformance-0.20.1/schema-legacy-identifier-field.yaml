# rejectedBy: schema
# errorAt: /
# The pre-0.20.0 field name for an entry's own id. Pins that using the old
# name alone (with no "id") still fails on the new required field, rather
# than being silently accepted as an unrelated extra property.
identifier: acme-design-system
kind: system
name: Acme Design System
description: A design system entry using the field name 0.20.0 renamed to "id".
