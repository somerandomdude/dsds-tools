# expect: DSDS-02
# rejectedBy: semantic
schemaVersion: "0.20.1"
name: Bad Platform Vocabulary
entries:
  - kind: system
    id: acme-design-system
    name: Acme Design System
    description: The design system as a whole.
    metadata:
      platforms: [react]
  - kind: component
    id: bad-entry
    name: Bad Entry
    description: Declares a sourceFiles entry for a platform not in the system entry's metadata.platforms.
    sourceFiles:
      - platform: vue
        file: ./src/BadEntry.vue
