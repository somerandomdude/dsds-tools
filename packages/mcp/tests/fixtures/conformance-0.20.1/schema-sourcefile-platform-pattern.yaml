kind: component
# rejectedBy: schema
# errorAt: /sourceFiles/0/platform
id: bad-platform
name: Bad Platform
description: A component whose sourceFiles platform uses capitals and a space, which the id pattern (lowercase, dash-separated) doesn't allow.
sourceFiles:
  - platform: React Native
    file: ./src/Foo.tsx
