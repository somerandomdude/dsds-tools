# rejectedBy: schema
# A DSDS <=0.15.2 base document, using the field name 0.20.0 renamed to
# schemaVersion. Pins that this gets a targeted "renamed" diagnostic
# instead of being silently misrouted into entry validation.
dsdsVersion: "0.15.2"
name: Legacy Version Field
entries:
  - identifier: acme-design-system
    kind: system
    name: Acme Design System
