kind: component
# rejectedBy: schema
# errorAt: /sourceFiles
id: bad-empty-sourcefiles
name: Bad Empty SourceFiles
description: A component declaring sourceFiles as an empty array, which minItems 1 doesn't allow - omit the field entirely instead of listing zero platforms.
sourceFiles: []
