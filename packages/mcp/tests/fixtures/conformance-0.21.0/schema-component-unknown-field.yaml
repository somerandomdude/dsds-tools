kind: component
# rejectedBy: schema
# errorAt: /
id: bad-unknown-field
name: Bad Unknown Field
description: A component with a top-level field that isn't part of the entry envelope or the component-specific fields.
bogusField: true
