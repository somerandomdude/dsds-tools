kind: component
# rejectedBy: schema
# errorAt: /
# documentBlocks/agentDocumentBlocks were replaced by one sections[]
# array (tagged for: human|agent|all) in 0.20.0. Pins that the old field
# name is rejected outright on a closed kind, not silently accepted as an
# extra property alongside the real `sections`.
id: bad-legacy-blocks
name: Bad Legacy Blocks
description: A component using the pre-0.20.0 documentBlocks field instead of sections.
documentBlocks:
  - kind: use-cases
    items: []
