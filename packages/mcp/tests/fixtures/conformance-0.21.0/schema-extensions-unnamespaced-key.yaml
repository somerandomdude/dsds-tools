kind: entry
# rejectedBy: schema
# errorAt: /$extensions
id: bad-extensions-key
name: Bad Extensions Key
description: An entry whose $extensions key isn't a dotted namespace, which the Design Tokens Community Group convention this schema follows requires.
$extensions:
  acme:
    displayName: Not namespaced
