kind: component
# rejectedBy: schema
id: bad-trait-missing-kind
name: Bad Trait Missing Kind
description: "A component trait with neither `kind: boolean` nor `kind: enum` - the tag that says which of the two kinds applies."
traits:
  - id: loading
    traitType: state
    description: Shows a spinner in place of the label.
