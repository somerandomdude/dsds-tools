{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.13.0/dsds.bundled.schema.json",
  "title": "Design System Documentation Spec (DSDS) v0.13.0 — Bundled",
  "description": "Single-file bundled version of the DSDS schema. Auto-generated from the split schema files by scripts/bundle.js. Use the split files for development; use this file for tools that require a single schema document.",
  "type": "object",
  "required": [
    "dsdsVersion"
  ],
  "oneOf": [
    {
      "required": [
        "entityGroups"
      ]
    },
    {
      "required": [
        "entity"
      ]
    }
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "URI reference to the DSDS JSON Schema for validation."
    },
    "dsdsVersion": {
      "type": "string",
      "const": "0.13.0",
      "description": "The DSDS spec version this document conforms to."
    },
    "systemInfo": {
      "$ref": "#/$defs/systemInfo"
    },
    "extends": {
      "$ref": "#/$defs/documentExtends",
      "description": "Declares that this DSDS document inherits from another DSDS document — usually a core design system. The base document provides the core entities. This document layers on top, adding or extending entities. It supports systems-of-systems architectures, where product- or brand-specific systems inherit from a shared core."
    },
    "documentBlocks": {
      "type": "array",
      "description": "System-level structured documentation for the design system as a whole — its purpose (when to adopt the system), cross-cutting guidelines, and narrative overviews. Each entry is a general document block: a `useCases`, `guidelines`, `sections`, `accessibility`, `content`, or `checklist` block. This is the same `documentBlocks` mechanism entities use, scoped to the system rather than a single artifact. Position signals the scope (root vs. inside an entity).",
      "items": {
        "$ref": "#/$defs/generalDocumentBlock"
      },
      "minItems": 1
    },
    "entityGroups": {
      "type": "array",
      "description": "One or more entity groups. Each group is a named collection with a single `entities` array. That array holds any mix of entity types — components, tokens, token groups, themes, foundations, patterns, guides, and chunks — in display order. Each entity's `kind` identifies its type. Multiple groups let one DSDS file organize entities into logical sections (e.g., one group for foundations, another for components). Groups may be inlined or referenced through a $ref object that points to another DSDS file.",
      "items": {
        "oneOf": [
          {
            "$ref": "#/$defs/entityGroup"
          },
          {
            "$ref": "#/$defs/fileRef"
          }
        ]
      },
      "minItems": 1
    },
    "entity": {
      "$ref": "#/$defs/anyEntity",
      "description": "A single entity document — one component, token, token group, theme, foundation, pattern, guide, or chunk. Use this instead of `entityGroups` when each entity lives in its own file. The `kind` field on the entity identifies its type."
    },
    "$extensions": {
      "$ref": "#/$defs/extensions"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "conformanceLevel": {
      "type": "string",
      "enum": [
        "must",
        "should",
        "should-not",
        "must-not"
      ],
      "description": "Conformance level, named for RFC 2119 requirement levels: 'must' and 'must-not' are absolute requirements (non-compliance is a defect); 'should' and 'should-not' are strong defaults that valid exceptions may override when the implications are understood. Values are lowercase kebab-case like every DSDS-native vocabulary — in prose, the keywords stay capitalized (MUST, SHOULD NOT) where they carry conformance weight. Shared by guideline entries and criteria so reports aggregate on one severity vocabulary."
    },
    "verificationMode": {
      "type": "string",
      "enum": [
        "automated",
        "assisted",
        "manual"
      ],
      "description": "How a result for this criterion is produced — who decides the outcome. 'automated': a runner fully decides; a `check` MUST be present. 'assisted': a tool surfaces candidates but a human decides; `check` diagnostics are advisory. 'manual': pure human judgment; no `check` applies — `techniques` and `failures` serve as the reviewer's procedure. This is a closed set: the axis is who decides, and named procedures (e.g. a design review) are the 'how', which belongs in `techniques`. When omitted, tools MUST NOT assume the criterion is automatable."
    },
    "criterionCheck": {
      "type": "object",
      "description": "A dispatch envelope for automated or assisted verification. DSDS defines only the envelope: `scheme` names the runner or rule system (open vocabulary, e.g. 'axe-core', 'vitest', 'stylelint', 'lighthouse'); all other properties are scheme-specific configuration owned by the named tool. A runner that does not recognize the scheme MUST report the criterion as skipped, never as passing. Like `extensions`, this object is intentionally open: execution semantics live in tools, not in this specification.",
      "required": [
        "scheme"
      ],
      "properties": {
        "scheme": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9.-]*$",
          "description": "Identifier of the rule system or runner this check dispatches to. MUST be lowercase; dots permitted for namespacing (e.g. 'com.acme.contract-tests')."
        }
      },
      "additionalProperties": true
    },
    "criterionFixture": {
      "type": "object",
      "description": "An example with a declared outcome, used as a verification fixture for a criterion. Identical to `example` plus a required `outcome`. NOTE: this is an intentional copy of the `example` shape, not a composition — `example` is a closed schema (additionalProperties: false), so allOf-extension cannot add `outcome`. When `example` changes, change this definition to match (example.schema.json carries the mirror note). Fixtures make criteria self-verifying: a conformance runner SHOULD execute the criterion's `check` (when present) against each fixture and confirm the declared outcome is reproduced — a 'fail' fixture that passes means the check no longer detects what it claims to.",
      "anyOf": [
        {
          "required": [
            "presentation",
            "outcome"
          ]
        },
        {
          "required": [
            "value",
            "outcome"
          ]
        }
      ],
      "properties": {
        "title": {
          "type": "string",
          "description": "A human-readable caption for the example (e.g., 'Primary button in default state'). For API property examples, use `value` for the literal content and `title` for the display label."
        },
        "description": {
          "type": "string",
          "description": "An explanation of the example — why it is encouraged, discouraged, or notable."
        },
        "value": {
          "description": "A literal value carried by the example. Use it for API property examples, where the content is a concrete value rather than a visual demo. It can display code that reflects the example. Accepts any JSON type: string, number, boolean, object, array, or null. When given without a presentation, this value alone is the example. Do not use `value` for display text — use `title` for captions and `description` for explanations."
        },
        "presentation": {
          "description": "The visual or interactive presentation for the example. Required when no `value` is set. Optional when `value` is present. Value-only examples are concrete data points that tools render as table rows or inline displays.",
          "oneOf": [
            {
              "$ref": "#/$defs/presentationImage"
            },
            {
              "$ref": "#/$defs/presentationVideo"
            },
            {
              "$ref": "#/$defs/presentationCode"
            },
            {
              "$ref": "#/$defs/presentationUrl"
            }
          ]
        },
        "outcome": {
          "type": "string",
          "enum": [
            "pass",
            "fail"
          ],
          "description": "Whether this fixture satisfies ('pass') or violates ('fail') the criterion. Replaces encoding pass/fail in `title` prose, which tools cannot rely on."
        }
      },
      "additionalProperties": false
    },
    "reference": {
      "type": "object",
      "description": "A citation of an external standard or requirement. This is a published rule the docs address (WCAG, platform guidelines, vendor compliance frameworks).",
      "required": [
        "url"
      ],
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "description": "URL to the external standard or requirement (e.g., 'https://www.w3.org/TR/WCAG22/#contrast-minimum')."
        },
        "label": {
          "type": "string",
          "description": "A display label for the citation (e.g., '1.4.3 Contrast (Minimum)', 'WCAG 2.5.8 Target Size'). When omitted, tools MAY build a label from the URL."
        }
      },
      "additionalProperties": false
    },
    "criterion": {
      "type": "object",
      "description": "A testable success criterion: an objectively verifiable condition an implementation must meet. Criteria let documentation act as a test harness — a result is produced per `verification` mode and reported under the criterion's `identifier`. `statement` is the normative, human-readable source of truth; `check`, when present, is its machine-dispatchable shadow and never replaces it. Pair with `techniques` (how to pass), `failures` (what failing looks like), `references` (external standards), and `examples` (fixtures with declared outcomes).",
      "required": [
        "identifier",
        "statement"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Stable identifier for this criterion, unique within the parent entity (e.g., 'touch-target-minimum', 'single-primary-action'). MUST be lowercase kebab-case. Test runs report pass/fail against this identifier, so it SHOULD NOT change once checks depend on it — renaming breaks result history across runs."
        },
        "title": {
          "type": "string",
          "description": "A short display name for the criterion (e.g., 'Touch Target Minimum'). Tools SHOULD render it in docs and test reports beside the identifier."
        },
        "statement": {
          "$ref": "#/$defs/richText",
          "description": "The testable statement of what success looks like. MUST be objectively verifiable — by inspection, static analysis, or runtime measurement. Avoid vague language like 'feels right' or 'looks balanced' (e.g., 'Interactive elements present a hit area of at least 44×44 px')."
        },
        "techniques": {
          "type": "array",
          "description": "Sufficient ways to satisfy this criterion — the passing patterns (e.g., 'Apply `min-height: 44px` to every interactive element', 'Use the `<Button>` component without overriding its default height').",
          "items": {
            "$ref": "#/$defs/richText"
          },
          "minItems": 1
        },
        "failures": {
          "type": "array",
          "description": "Known failure modes — conditions that count as failing this criterion (e.g., 'Setting `height` below 44px on an interactive element', 'Removing `aria-label` from an icon-only button').",
          "items": {
            "$ref": "#/$defs/richText"
          },
          "minItems": 1
        },
        "references": {
          "type": "array",
          "description": "External standards this criterion aligns with or derives from (e.g., a matching WCAG success criterion, a platform guideline).",
          "items": {
            "$ref": "#/$defs/reference"
          },
          "minItems": 1
        },
        "examples": {
          "type": "array",
          "description": "Verification fixtures — typically one passing and one failing implementation, each with a declared `outcome`.",
          "items": {
            "$ref": "#/$defs/criterionFixture"
          },
          "minItems": 1
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Freeform keywords that relate criteria across guidelines and entities (e.g., 'a11y', 'contrast', 'touch-target')."
        },
        "level": {
          "$ref": "#/$defs/conformanceLevel",
          "description": "The conformance level of this criterion. A criterion referenced by a guideline SHOULD inherit the guideline's `level` when omitted; a standalone criterion SHOULD declare its own."
        },
        "verification": {
          "$ref": "#/$defs/verificationMode"
        },
        "check": {
          "$ref": "#/$defs/criterionCheck"
        },
        "since": {
          "type": "string",
          "description": "The design system version in which this criterion was introduced (e.g., '1.0.0', '2.3.0'). Criterion identifiers are stable: once published, an identifier MUST NOT be reused for a different requirement — retire the identifier and mint a new one instead."
        }
      },
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "properties": {
              "verification": {
                "const": "automated"
              }
            },
            "required": [
              "verification"
            ]
          },
          "then": {
            "required": [
              "check"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "verification": {
                "const": "manual"
              }
            },
            "required": [
              "verification"
            ]
          },
          "then": {
            "not": {
              "required": [
                "check"
              ]
            }
          }
        },
        {
          "if": {
            "required": [
              "check"
            ]
          },
          "then": {
            "required": [
              "verification"
            ]
          }
        }
      ]
    },
    "isoDate": {
      "type": "string",
      "format": "date",
      "description": "An ISO 8601 date (YYYY-MM-DD).",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "plainNote": {
      "type": "string",
      "minLength": 1,
      "description": "A plain-text note. MUST NOT contain markup and MUST NOT be empty."
    },
    "entityIdentifier": {
      "type": "string",
      "description": "The identifier of a documented DSDS entity (e.g., 'button', 'form-field', 'color-text-primary'). MUST match that entity's `identifier`. Tools SHOULD resolve it against the entity catalog to build navigable cross-references; a reference that resolves to nothing is a defect in the document.",
      "minLength": 1
    },
    "entityRole": {
      "type": "string",
      "description": "The role the referenced entity plays in this context (e.g., 'Carries the inline error', 'Submit, disabled while invalid'). When omitted, the reference is a general association."
    },
    "entityRef": {
      "type": "object",
      "description": "A reference to another entity by identifier, with an optional role describing what the entity does in this context. Used where a position points at an entity and nothing else; for pointers that may also carry a URL or external-resource kind, use `link`.",
      "required": [
        "identifier"
      ],
      "properties": {
        "identifier": {
          "$ref": "#/$defs/entityIdentifier"
        },
        "role": {
          "$ref": "#/$defs/entityRole"
        }
      },
      "additionalProperties": false
    },
    "example": {
      "type": "object",
      "description": "A single example. It needs at least a presentation or a value (or both). The presentation shows a visual or interactive demo. The value carries a literal for API property examples. Optional title and description add context. When only a value is present, the example is a data point (e.g., a prop value of 'primary' or a token value of 44). Tools SHOULD render value-only examples as table rows or inline displays. NOTE: `criterionFixture` in criterion.schema.json is an intentional copy of this shape plus a required `outcome` (closed schemas prevent allOf-extension). When this definition changes, change criterionFixture to match.",
      "anyOf": [
        {
          "required": [
            "presentation"
          ]
        },
        {
          "required": [
            "value"
          ]
        }
      ],
      "properties": {
        "title": {
          "type": "string",
          "description": "A human-readable caption for the example (e.g., 'Primary button in default state'). For API property examples, use `value` for the literal content and `title` for the display label."
        },
        "description": {
          "type": "string",
          "description": "An explanation of the example — why it is encouraged, discouraged, or notable."
        },
        "value": {
          "description": "A literal value carried by the example. Use it for API property examples, where the content is a concrete value rather than a visual demo. It can display code that reflects the example. Accepts any JSON type: string, number, boolean, object, array, or null. When given without a presentation, this value alone is the example. Do not use `value` for display text — use `title` for captions and `description` for explanations."
        },
        "presentation": {
          "description": "The visual or interactive presentation for the example. Required when no `value` is set. Optional when `value` is present. Value-only examples are concrete data points that tools render as table rows or inline displays.",
          "oneOf": [
            {
              "$ref": "#/$defs/presentationImage"
            },
            {
              "$ref": "#/$defs/presentationVideo"
            },
            {
              "$ref": "#/$defs/presentationCode"
            },
            {
              "$ref": "#/$defs/presentationUrl"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "documentExtends": {
      "type": "object",
      "description": "Says that this DSDS document inherits from another DSDS document. The base document supplies the core entities: components, tokens, themes, foundations, and patterns. This document layers on top, adding new entities or extending base ones. The declaration sets up the relationship. The consuming tools handle merge and resolution. A common case: a product-specific system that inherits from an organization's core design system.",
      "required": [
        "system"
      ],
      "properties": {
        "system": {
          "type": "string",
          "description": "The name or identifier of the base design system being extended (e.g., 'Acme Core Design System', 'acme-core', '@acme/design-system'). A display name or a package name, not a URL. Tools use it to resolve the base system from a registry, file system, or package manager."
        },
        "url": {
          "type": "string",
          "format": "uri-reference",
          "description": "URL or relative path to the base DSDS document (e.g., 'https://design.acme.com/v2/core.dsds.json', './core.dsds.json'). Relative paths are resolved against this document's location. When provided, tools MAY fetch and resolve the base document for merge, validation, or docs generation."
        },
        "version": {
          "type": "string",
          "description": "The version of the base system this document extends (e.g., '2.0.0', '>=2.0.0', '^3.1.0'). Follows semver. When omitted, tools SHOULD resolve to the latest available version."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "Description of the relationship: what this extension adds, changes, or narrows relative to the base system."
        }
      },
      "additionalProperties": false
    },
    "entityExtends": {
      "type": "object",
      "description": "Says that this entity inherits from a base entity in a parent design system. The base entity supplies the core definition: identity, anatomy, API, variants, states, guidelines, and so on. This entity layers on top, adding properties, guidelines, or special behavior. For example, an enterprise Button might extend the core Button. It could add variants, accessibility requirements, or API props while inheriting the core's anatomy, states, and base guidelines. The declaration sets up the relationship. The consuming tools handle merge and resolution.",
      "required": [
        "identifier"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "The identifier of the base entity being extended (e.g., 'button', 'color-text-primary', 'form-validation'). MUST match the `identifier` property of the base entity in the parent system."
        },
        "system": {
          "type": "string",
          "description": "The name or identifier of the system that owns the base entity (e.g., 'Acme Core Design System', 'acme-core'). When omitted, the base entity is resolved from the system declared in the root document's `extends` property. When provided, it lets you extend from a system other than the document's default base."
        },
        "url": {
          "type": "string",
          "format": "uri-reference",
          "description": "URL or relative path to the base entity's docs or DSDS definition. Useful for cross-linking in docs sites."
        },
        "version": {
          "type": "string",
          "description": "The version of the base entity or base system this entity extends (e.g., '2.0.0'). When omitted, the version comes from the document-level extends declaration."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this entity adds or changes relative to the base: new variants, more props, overridden guidelines, and so on."
        },
        "modifications": {
          "type": "array",
          "description": "Summary of what this entity changes relative to the base. Each entry describes one change. Tools MAY use these for changelog generation, diff views, or migration guides. When omitted, consuming tools must diff the entities to find the changes.",
          "items": {
            "type": "object",
            "required": [
              "changeType",
              "description"
            ],
            "properties": {
              "changeType": {
                "type": "string",
                "enum": [
                  "added",
                  "modified",
                  "removed",
                  "inherited"
                ],
                "description": "The kind of change: 'added' (not in the base), 'modified' (changed from the base), 'removed' (in the base but dropped here), 'inherited' (carried from the base unchanged — usually omitted, since inheritance is the default, but useful for docs)."
              },
              "target": {
                "type": "string",
                "description": "What changed: a property name, guideline kind, variant name, or other identifier (e.g., 'variant:enterprise', 'prop:theme', 'guideline:accessibility', 'state:read-only')."
              },
              "description": {
                "$ref": "#/$defs/richText",
                "description": "A short description of the change."
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "extensions": {
      "type": "object",
      "description": "Vendor-specific extensions. Keys MUST use vendor-specific namespaces (reverse domain name notation is recommended) — at least two dot-separated segments, e.g. 'com.figma', 'acme.tooling'. The pattern is deliberately case-tolerant — vendors write reverse-domain namespaces as-is — unlike custom relation types, which use the stricter lowercase grammar. Tools that do not recognize an extension MUST keep it. Extension data SHOULD NOT duplicate core schema fields.",
      "propertyNames": {
        "pattern": "^[A-Za-z0-9_-]+(\\.[A-Za-z0-9_-]+)+$"
      },
      "additionalProperties": true
    },
    "link": {
      "type": "object",
      "description": "A typed pointer to an external resource. A link points to an external resource by `url` (source code, design files, docs, packages). The `kind` property sorts the reference. Standard external kinds: 'source', 'design', 'storybook', 'documentation', 'package', 'repository', 'manifest' (a machine-readable component manifest, e.g. a Custom Elements Manifest). Custom values are allowed. DEPRECATED — using a link to express how entities relate is deprecated in favor of the entity's `relationships` array, which carries typed, validated edges. This covers the relationship kinds ('alternative', 'parent', 'child', 'related') and internal artifact references by `identifier` ('component', 'token', 'token-group', 'foundation', 'pattern', 'theme'). Those forms still validate for backward compatibility, but tools SHOULD warn (DSDS-010) and authors SHOULD migrate them to `relationships`. Reserve `link` for external resources addressed by `url`.",
      "required": [
        "kind"
      ],
      "anyOf": [
        {
          "required": [
            "url"
          ]
        },
        {
          "required": [
            "identifier"
          ]
        }
      ],
      "properties": {
        "kind": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "The category of the linked resource. Standard external kinds: 'source', 'design', 'storybook', 'documentation', 'package', 'repository'. Custom values are allowed and MUST match ^[a-z][a-z0-9-]*$ (schema-enforced; all standard kinds already do). DEPRECATED kinds — 'alternative', 'parent', 'child', 'related' (relationship kinds) and 'component', 'token', 'token-group', 'foundation', 'pattern', 'theme' (internal artifact references) are deprecated; express these as typed edges in the entity's `relationships` array instead."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the linked resource. MUST be a valid absolute URI. Required for external resource links. Optional for internal artifact references, where `identifier` names the artifact. When both `url` and `identifier` are present, `url` points to the artifact's docs page and `identifier` is the machine-readable cross-reference."
        },
        "identifier": {
          "$ref": "#/$defs/entityIdentifier",
          "description": "DEPRECATED — the identifier of a referenced DSDS entity. Use the entity's `relationships` array for inter-entity references. Retained for backward compatibility: a link with an `identifier` still follows entityRef resolution rules (it MUST match a documented entity)."
        },
        "label": {
          "type": "string",
          "description": "Display text for this link — what a user sees when the link renders (e.g., 'React component source', 'Design file — variants', 'Button component'). Use it for the display name of the resource. To describe an internal artifact's role, use `role` instead."
        },
        "role": {
          "$ref": "#/$defs/entityRole",
          "description": "The role the referenced entity plays in the current entity (e.g., 'Primary action trigger', 'Provides semantic text color tokens'). Distinct from `label`, which is display text for the link itself. When omitted, the link is a general association."
        },
        "required": {
          "type": "boolean",
          "default": false,
          "description": "DEPRECATED — whether the referenced artifact is required for the parent entity to work. Use `required` on a `relationships` edge instead. Retained for backward compatibility on internal artifact references. Defaults to false."
        }
      },
      "additionalProperties": false
    },
    "mediaUrl": {
      "type": "string",
      "format": "uri-reference",
      "description": "URL or relative path to a media file. Absolute URLs point at hosted assets; relative paths are resolved against the referencing document's location."
    },
    "mediaAlt": {
      "type": "string",
      "minLength": 1,
      "description": "Alternative text describing the media content for assistive technology. MUST be provided and MUST NOT be empty."
    },
    "presentationImage": {
      "type": "object",
      "description": "An image presentation — a screenshot, diagram, annotated mockup, or exported design frame.",
      "required": [
        "kind",
        "url",
        "alt"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "image",
          "description": "Identifies this presentation as an image."
        },
        "url": {
          "$ref": "#/$defs/mediaUrl",
          "description": "URL or relative path to the image file."
        },
        "alt": {
          "$ref": "#/$defs/mediaAlt",
          "description": "Alternative text describing the image content. MUST be provided for accessibility and MUST NOT be empty."
        }
      },
      "additionalProperties": false
    },
    "presentationVideo": {
      "type": "object",
      "description": "A video presentation — a screen recording, animated transition, or interaction walkthrough.",
      "required": [
        "kind",
        "url",
        "alt"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "video",
          "description": "Identifies this presentation as a video."
        },
        "url": {
          "$ref": "#/$defs/mediaUrl",
          "description": "URL or relative path to the video file."
        },
        "alt": {
          "$ref": "#/$defs/mediaAlt",
          "description": "Alternative text describing the video content. MUST be provided for accessibility and MUST NOT be empty."
        }
      },
      "additionalProperties": false
    },
    "presentationCode": {
      "type": "object",
      "description": "A code presentation — a source code snippet showing how to use the artifact.",
      "required": [
        "kind",
        "code",
        "language"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "code",
          "description": "Identifies this presentation as a code snippet."
        },
        "code": {
          "type": "string",
          "description": "The source code of the example.",
          "minLength": 1
        },
        "language": {
          "type": "string",
          "description": "The programming language or syntax (e.g., 'jsx', 'tsx', 'html', 'css', 'vue', 'svelte', 'swift', 'kotlin')."
        }
      },
      "additionalProperties": false
    },
    "presentationUrl": {
      "type": "object",
      "description": "A URL presentation — a link to a web resource such as a live demo, interactive example, docs page, or hosted component story. Use it for Storybook stories, CodeSandbox embeds, StackBlitz projects, or other addressable web content.",
      "required": [
        "kind",
        "url"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "url",
          "description": "Identifies this presentation as a URL reference to a web resource."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "URL to the web resource (e.g., 'https://storybook.acme.com/?path=/story/components-button--primary', 'https://codesandbox.io/s/button-example')."
        }
      },
      "additionalProperties": false
    },
    "relationType": {
      "description": "The kind of relationship, in its canonical direction (source → target). Standard relations: 'depends-on' (the source needs the target to function — e.g. a component depends on a token), 'composes' (the source is built from the target — e.g. a pattern composes a component), 'part-of' (the source is a member of the target — e.g. a token is part of a token group), 'alternative-to' (the source is an interchangeable option for the target; symmetric), 'replaces' (the source supersedes a deprecated target), 'extends' (the source inherits from the target). Tools derive the inverse edges ('composed-by', 'replaced-by', 'contains', 'dependency-of', 'extended-by') and MUST NOT require them to be authored. Custom relations are allowed and MUST be vendor-namespaced (e.g. 'acme.themes').",
      "anyOf": [
        {
          "type": "string",
          "enum": [
            "depends-on",
            "composes",
            "part-of",
            "alternative-to",
            "replaces",
            "extends"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-z0-9-]+(\\.[a-z0-9-]+)+$"
        }
      ]
    },
    "relationship": {
      "type": "object",
      "description": "A single directional edge from the entity that declares it (the source) to another documented entity (the target). Pairs a controlled `relation` with the target's `identifier`. `required` marks whether the target is needed for the source to work (meaningful for 'depends-on' and 'composes'). `versionConstraint` pins a compatible range of the target. `role` describes what the target does in this relationship.",
      "required": [
        "relation",
        "target"
      ],
      "properties": {
        "relation": {
          "$ref": "#/$defs/relationType"
        },
        "target": {
          "$ref": "#/$defs/entityIdentifier",
          "description": "The identifier of the entity this edge points at. MUST match a documented entity's `identifier`. A target that resolves to nothing is a defect in the document."
        },
        "role": {
          "$ref": "#/$defs/entityRole",
          "description": "What the target does in this relationship (e.g., 'Provides semantic text color', 'Renders the confirm action'). When omitted, the edge is a general association of the given relation type."
        },
        "required": {
          "type": "boolean",
          "default": false,
          "description": "Whether the target is required for the source entity to function. Meaningful for 'depends-on' and 'composes' edges; ignored for symmetric or advisory relations like 'alternative-to'. Defaults to false."
        },
        "versionConstraint": {
          "type": "string",
          "description": "An optional version range that the target must satisfy for this relationship to hold, expressed as a semver range (e.g., '>=2.0.0', '^3'). Use it when a dependency is tied to a specific version of the target. Tools treat it as advisory metadata, not a hard validation rule."
        }
      },
      "additionalProperties": false
    },
    "relationships": {
      "type": "array",
      "description": "The entity's outgoing relationship edges, in canonical direction. Each entry is a typed `relationship` to another documented entity. Order is not significant. Tools derive inverse edges (e.g., 'composed-by') and the whole-graph view; authors declare each edge once, on the source. Use this for the structural dependency graph; use the `links` metadata field for external resources and display-oriented pointers.",
      "items": {
        "$ref": "#/$defs/relationship"
      },
      "minItems": 1
    },
    "richText": {
      "type": "string",
      "description": "Human-written prose, read as CommonMark markdown (0.27 minimum). No format flag is needed. Plain sentences and inline HTML are both valid markdown, so write plain text, markdown, or embedded HTML as the content needs. Tools MUST render the value as markdown.",
      "minLength": 1
    },
    "statusValue": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "A lifecycle status value. MUST be lowercase kebab-case. Standard values: 'draft' (under development, not ready for use), 'experimental' (available, but the API may change without notice), 'stable' (production-ready; changes follow semver), 'deprecated' (scheduled for removal). Custom values are allowed (e.g., 'sunset', 'archived', 'beta') and MUST follow the same pattern."
    },
    "deprecationNotice": {
      "type": "string",
      "minLength": 1,
      "description": "A non-empty notice for a deprecated artifact. MUST say what to use instead and, where one exists, give a migration path. Shared by the platform-level and entity-level status forms so the non-empty rule lives in one place."
    },
    "platformStatus": {
      "type": "object",
      "description": "Readiness of an artifact on one platform. Tracks the lifecycle stage, the version where it became available, and an optional deprecation notice.",
      "required": [
        "status"
      ],
      "properties": {
        "status": {
          "$ref": "#/$defs/statusValue",
          "description": "Lifecycle status of the artifact on this platform. Uses the same vocabulary as the overall status: 'draft', 'experimental', 'stable', 'deprecated'. Custom values are allowed."
        },
        "since": {
          "type": "string",
          "description": "The version in which this artifact became available on this platform."
        },
        "deprecationNotice": {
          "$ref": "#/$defs/deprecationNotice",
          "description": "Required when status is 'deprecated'. Says what to use instead on this platform. MUST NOT be empty."
        },
        "description": {
          "type": "string",
          "description": "Notes about the artifact's status on this platform (e.g., 'Available as a Web Component wrapper; native implementation planned for v4')."
        }
      },
      "if": {
        "properties": {
          "status": {
            "const": "deprecated"
          }
        }
      },
      "then": {
        "required": [
          "deprecationNotice"
        ]
      },
      "additionalProperties": false
    },
    "systemInfo": {
      "type": "object",
      "description": "System-level identity and provenance for the design system.",
      "required": [
        "systemName"
      ],
      "properties": {
        "systemName": {
          "type": "string",
          "description": "Display name of the design system (e.g., 'Acme Design System'). A human-readable name, not a package name — package identifiers belong in `extends.system` or platform imports."
        },
        "systemVersion": {
          "type": "string",
          "description": "Version of the design system this documentation describes (e.g., '2.3.0'). SHOULD follow semver so tools can compare against `extends.version` and `reviewedAgainst` values."
        },
        "organization": {
          "type": "string",
          "description": "The organization that maintains the system."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "URL to the system's documentation site."
        },
        "license": {
          "type": "string",
          "description": "License of the design system, as an SPDX identifier (e.g., 'MIT', 'Apache-2.0') or a URL to the license text."
        }
      },
      "additionalProperties": false
    },
    "tokenOverrides": {
      "type": "object",
      "minProperties": 1,
      "description": "Design token overrides for one part, variant value, or state. Keys are token-purpose names describing the visual attribute the token controls (e.g., 'background', 'text-color', 'border-radius', 'padding-horizontal'). Values are token `identifier` references (e.g., 'color-action-primary', 'radius-md') and follow the same resolution rule as every entity reference: in a system that documents its token layer, they MUST resolve to a documented token. Validators resolve them when a token layer is present (see entityRef resolution). Raw values (e.g., '#ffffff', '4px') are not valid here; token-less systems document raw values in design-specifications, not in token-override maps.",
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*$"
      },
      "additionalProperties": {
        "$ref": "#/$defs/entityIdentifier"
      },
      "examples": [
        {
          "background": "color-action-primary",
          "text-color": "color-text-on-action",
          "border-radius": "radius-md"
        }
      ]
    },
    "useCase": {
      "type": "object",
      "description": "A single use case. It describes when an artifact or option is or is not a good fit.",
      "required": [
        "description",
        "stance"
      ],
      "properties": {
        "description": {
          "$ref": "#/$defs/richText",
          "description": "Describes the scenario. Should be concrete and situation-driven — describe the user's situation, not the artifact's features."
        },
        "stance": {
          "type": "string",
          "enum": [
            "recommended",
            "discouraged"
          ],
          "description": "Whether this scenario is one to follow ('recommended') or to avoid ('discouraged')."
        },
        "alternative": {
          "type": "object",
          "description": "The recommended alternative artifact for this scenario. Usually given for discouraged scenarios, to point the reader toward a better-suited component, pattern, or token.",
          "required": [
            "identifier"
          ],
          "properties": {
            "identifier": {
              "$ref": "#/$defs/entityIdentifier",
              "description": "The identifier of the recommended alternative artifact (e.g., 'link', 'radio-group', 'icon-button', 'empty-state')."
            },
            "rationale": {
              "$ref": "#/$defs/richText",
              "description": "Why the alternative is a better fit for this scenario. Cite semantic differences, accessibility impact, or UX rationale when available."
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "useCases": {
      "type": "object",
      "description": "Documents what an artifact is used for — when it is the right choice and when to choose something else. The optional `purpose` statement is the umbrella answer: a short summary of what the artifact is for, framing the use cases beneath it. `items` holds scenario-driven `useCase` entries, each marked 'recommended' (when to use) or 'discouraged' (when to pick an alternative). Use `useCases` to answer *whether* to use the artifact. For *how* to use it correctly, see `guidelines`. For the higher-level beliefs that shape a domain, see `principles`. For *what* it looks like and how it behaves, see variants, states, and anatomy.",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "useCases",
          "description": "Identifies this document block as a use-cases spec."
        },
        "purpose": {
          "$ref": "#/$defs/richText",
          "description": "The umbrella purpose statement for the artifact — one or two sentences saying what it is for, framing the use cases in `items` (e.g., 'Buttons trigger immediate actions. They are the primary mechanism for initiating changes within a surface.')."
        },
        "items": {
          "type": "array",
          "description": "Scenario-driven use cases. They describe when this artifact is or is not the right choice. Order matters: tools SHOULD keep it for display. Authors SHOULD lead with recommended scenarios.",
          "items": {
            "$ref": "#/$defs/useCase"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "aliases": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1,
      "description": "Alternative names for this entity across teams, tools, or legacy systems. Used for search, migration, and cross-referencing.",
      "uniqueItems": true
    },
    "category": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Where this entity fits in the design system's taxonomy (e.g., 'action', 'navigation', 'feedback', 'base', 'semantic'). MUST be lowercase kebab-case."
    },
    "authorshipValue": {
      "type": "string",
      "description": "Who or what wrote the documentation's words. Orthogonal to origin: origin says what the doc was produced from; authorship says what produced it. 'human': written by a person with no AI or script involvement. 'ai-assisted': human-led, with an AI model drafting or editing portions under human direction and review. 'ai-generated': produced by an AI model or agent with at most light human review; MAY contain plausible-but-wrong statements and SHOULD be verified before an agent treats it as ground truth. 'machine-assisted': human-led, with deterministic tooling (scripts, doc generators) producing portions such as tables or listings. 'machine-generated': produced end-to-end by deterministic tooling with no human or AI synthesis; exactly as trustworthy as its source and pipeline. The AI values and the machine values differ in reproducibility: machine output is determined by its input; AI output is not. When both AI and scripts contributed, record the AI value — AI involvement is what consumers most need to know.",
      "enum": [
        "human",
        "ai-assisted",
        "ai-generated",
        "machine-assisted",
        "machine-generated"
      ]
    },
    "docOriginValue": {
      "type": "string",
      "description": "How the documentation was produced, relative to the implementation. 'authored': written before or alongside the implementation as its specification — the code follows the doc; authoritative for intent, but MAY lag the shipped API. 'generated': produced by automated tooling from source artifacts (type definitions, doc comments) without human synthesis; high fidelity to the API, silent on intent. 'extracted': derived by a human or agent reading the existing implementation (source, stories, tests) after the fact; describes what exists. 'reconstructed': written after the fact from institutional knowledge, without systematically consulting the implementation; lowest fidelity to the API and SHOULD be verified before an agent relies on it for API facts. Agents SHOULD prefer 'generated' and 'extracted' documentation for API accuracy and 'authored' documentation for design intent.",
      "enum": [
        "authored",
        "generated",
        "extracted",
        "reconstructed"
      ]
    },
    "docOrigin": {
      "description": "How this entity's documentation came to exist. A bare string sets the overall origin (e.g., 'extracted') and covers the common case. Use the object form when the document has mixed origins — a prop table extracted from code inside otherwise authored guidance — or when the origin needs explaining. Origin describes production, not quality: it records how the words got here so consumers can decide how much to trust which parts.",
      "oneOf": [
        {
          "$ref": "#/$defs/docOriginValue"
        },
        {
          "type": "object",
          "description": "Full docOrigin form: the overall origin, the authorship, an optional explanatory note, and per-block overrides. A document MAY be 'authored' overall while individual blocks were 'extracted' or 'generated'.",
          "required": [
            "overall"
          ],
          "properties": {
            "overall": {
              "$ref": "#/$defs/docOriginValue",
              "description": "The dominant origin of the document as a whole. When blocks differ, choose the origin of the document's substantive prose; record exceptions in `blocks`."
            },
            "authorship": {
              "$ref": "#/$defs/authorshipValue",
              "description": "Who or what wrote the document's words, as a single value for the document as a whole. When production was mixed, record the least-supervised method that contributed substantively (a mostly human doc with AI-drafted sections is 'ai-assisted'). Note the interaction with origin: an origin of 'generated' implies 'machine-generated' or 'ai-generated' authorship — the schema rejects other pairings when both fields are set."
            },
            "note": {
              "$ref": "#/$defs/plainNote",
              "description": "Plain-text note explaining the origin (e.g., 'Authored as the spec for the v3 rebuild; prop table regenerated from source on each release'). MUST NOT contain markup."
            },
            "blocks": {
              "type": "object",
              "description": "Per-block overrides for blocks whose origin or authorship differs from the document-level values. Keys are document block kinds (e.g., 'api', 'guidelines', 'accessibility') matching the `kind` of a block in this entity's `documentBlocks` or `agentDocumentBlocks`. A value is either a bare origin string (the common case) or an object carrying `origin` and/or `authorship` when the block's production method also differs — an authored, human-written doc whose prop table is AI-generated records that here. List only the exceptions. A key that matches no block in the entity is a defect in the document (validators resolve keys against the entity's blocks).",
              "minProperties": 1,
              "propertyNames": {
                "pattern": "^[a-z][a-zA-Z0-9-]*$"
              },
              "additionalProperties": {
                "oneOf": [
                  {
                    "$ref": "#/$defs/docOriginValue"
                  },
                  {
                    "type": "object",
                    "description": "Full per-block override: the block's origin and/or authorship, when either differs from the document-level value. At least one is required.",
                    "minProperties": 1,
                    "properties": {
                      "origin": {
                        "$ref": "#/$defs/docOriginValue",
                        "description": "This block's origin, when it differs from `overall`."
                      },
                      "authorship": {
                        "$ref": "#/$defs/authorshipValue",
                        "description": "This block's authorship, when it differs from the document-level `authorship`."
                      }
                    },
                    "additionalProperties": false
                  }
                ]
              }
            }
          },
          "if": {
            "properties": {
              "overall": {
                "const": "generated"
              }
            },
            "required": [
              "authorship"
            ]
          },
          "then": {
            "properties": {
              "authorship": {
                "enum": [
                  "ai-generated",
                  "machine-generated"
                ]
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "owner": {
      "description": "The party accountable for this entity's documentation. A bare string names the owner and covers the common case. Use the object form to add contact details. Owners SHOULD be teams, roles, or group aliases (e.g., 'Design Systems', '@acme/design-system') rather than named individuals — person-level ownership goes stale on turnover, which is the exact situation this field exists for. Tools SHOULD surface the owner wherever a reader might need to escalate a discrepancy.",
      "oneOf": [
        {
          "type": "string",
          "description": "Name or alias of the owning team, role, or group (e.g., 'Design Systems', '@acme/design-system').",
          "minLength": 1
        },
        {
          "type": "object",
          "description": "Full owner form: the owning party's name plus a contact route.",
          "required": [
            "name"
          ],
          "properties": {
            "name": {
              "type": "string",
              "description": "Name or alias of the owning team, role, or group (e.g., 'Design Systems'). SHOULD name a team or role, not an individual.",
              "minLength": 1
            },
            "contact": {
              "type": "string",
              "description": "How to reach the owner: a URL, email address, or channel reference (e.g., 'https://github.com/orgs/acme/teams/design-system', 'design-system@acme.com', '#design-system'). Freeform; tools SHOULD render it as a link when it parses as a URL or email.",
              "minLength": 1
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "lastReviewed": {
      "description": "When a human last confirmed this entity's documentation is accurate. A bare ISO 8601 date string ('2026-06-12') covers the common case. Use the object form to record who reviewed and what they concluded. Reviewing does not imply changing: a review that finds the doc correct updates `lastReviewed` and leaves `lastUpdated` alone. Tools MAY treat an old or absent `lastReviewed` as a staleness signal, and agents SHOULD prefer recently reviewed documentation when sources conflict.",
      "oneOf": [
        {
          "$ref": "#/$defs/isoDate",
          "description": "ISO 8601 date (YYYY-MM-DD) of the most recent review of this entity's documentation."
        },
        {
          "type": "object",
          "description": "Full lastReviewed form: the date plus who reviewed, what version it was verified against, and an optional note on the outcome.",
          "required": [
            "date"
          ],
          "properties": {
            "date": {
              "$ref": "#/$defs/isoDate",
              "description": "ISO 8601 date (YYYY-MM-DD) of the most recent review of this entity's documentation."
            },
            "reviewedBy": {
              "type": "string",
              "description": "Who performed the review — a team, role, or individual (e.g., 'Design Systems', 'jane.doe'). Unlike `owner`, naming an individual here is fine: it records a historical fact, not a standing responsibility.",
              "minLength": 1
            },
            "note": {
              "$ref": "#/$defs/plainNote",
              "description": "Plain-text note on the review outcome (e.g., 'Verified against v3.1; prop table still accurate', 'Guidelines section needs a rewrite — tracked in DS-412'). MUST NOT contain markup."
            },
            "reviewedAgainst": {
              "type": "string",
              "description": "The implementation version the documentation was verified against in this review, as a package version or release identifier (e.g., '@acme/ui@3.1.0', '3.1.0'). Lets tools detect drift: if the implementation has moved past this version, the documentation MAY be out of date even if recently reviewed. Nested inside the review record so it structurally cannot exist without one.",
              "minLength": 1
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "governance": {
      "type": "object",
      "description": "Accountability and review state for this entity's documentation. `owner` is required — a governance object that names no owner answers none of the questions the field exists for. `lastReviewed` is optional but SHOULD be set once a review process exists; its object form records who reviewed and which implementation version was verified (`reviewedAgainst`), so a tool can answer 'is this doc verified, against which version, and who vouches for it.'",
      "required": [
        "owner"
      ],
      "properties": {
        "owner": {
          "$ref": "#/$defs/owner"
        },
        "lastReviewed": {
          "$ref": "#/$defs/lastReviewed"
        }
      },
      "additionalProperties": false
    },
    "lastUpdated": {
      "description": "When this entity's documentation last changed. A bare ISO 8601 date string ('2026-05-28') covers the common case. Use the object form to add a note describing what changed.",
      "oneOf": [
        {
          "$ref": "#/$defs/isoDate",
          "description": "ISO 8601 date (YYYY-MM-DD) of the most recent update to this entity's documentation."
        },
        {
          "type": "object",
          "description": "Full lastUpdated form: the date plus a note describing what changed.",
          "required": [
            "date"
          ],
          "properties": {
            "date": {
              "$ref": "#/$defs/isoDate",
              "description": "ISO 8601 date (YYYY-MM-DD) of the most recent update to this entity's documentation."
            },
            "note": {
              "$ref": "#/$defs/plainNote",
              "description": "Plain-text note summarizing what changed in the most recent update (e.g., 'Added focus-visible guidance', 'Revised contrast requirements for inverse surfaces'). MUST NOT contain markup."
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "links": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/link"
      },
      "minItems": 1,
      "description": "Links to external resources. Examples: source code, design files, docs pages, packages. Expressing how entities relate through links is deprecated (DSDS-010) — use the entity's `relationships` array for typed edges."
    },
    "entityMetadata": {
      "type": "object",
      "description": "Optional metadata for an entity, keyed by field name. Include only the fields that apply. Covers lifecycle (status, since, lastUpdated), classification and discovery (category, tags, aliases), compact display (summary, thumbnail, preview), inheritance (extends), references (links), and documentation accountability and provenance (governance, docOrigin — both initial drafts).",
      "properties": {
        "status": {
          "$ref": "#/$defs/status"
        },
        "since": {
          "$ref": "#/$defs/since"
        },
        "lastUpdated": {
          "$ref": "#/$defs/lastUpdated"
        },
        "category": {
          "$ref": "#/$defs/category"
        },
        "tags": {
          "$ref": "#/$defs/tags"
        },
        "aliases": {
          "$ref": "#/$defs/aliases"
        },
        "summary": {
          "$ref": "#/$defs/summary"
        },
        "thumbnail": {
          "$ref": "#/$defs/thumbnail"
        },
        "preview": {
          "$ref": "#/$defs/preview"
        },
        "extends": {
          "$ref": "#/$defs/entityExtends"
        },
        "links": {
          "$ref": "#/$defs/links"
        },
        "governance": {
          "$ref": "#/$defs/governance"
        },
        "docOrigin": {
          "$ref": "#/$defs/docOrigin"
        }
      },
      "additionalProperties": false,
      "minProperties": 1
    },
    "preview": {
      "description": "A visual or interactive preview of the entity — an image, video, code snippet, or URL. The value is a presentation object. Its `kind` tag selects the media type.",
      "oneOf": [
        {
          "$ref": "#/$defs/presentationImage"
        },
        {
          "$ref": "#/$defs/presentationVideo"
        },
        {
          "$ref": "#/$defs/presentationCode"
        },
        {
          "$ref": "#/$defs/presentationUrl"
        }
      ]
    },
    "since": {
      "type": "string",
      "description": "The design system version in which this entity was introduced (e.g., '1.0.0', '2.3.0').",
      "minLength": 1
    },
    "status": {
      "description": "Lifecycle status of the entity. A bare string sets the overall status (e.g., 'stable', 'beta') and covers the common case. Use the object form to add per-platform readiness, an explanatory note, or a deprecation notice. A deprecated entity MUST use the object form, because deprecation needs a deprecationNotice that says what to use instead.",
      "oneOf": [
        {
          "$ref": "#/$defs/statusValue",
          "not": {
            "const": "deprecated"
          }
        },
        {
          "type": "object",
          "description": "Full status form: overall lifecycle value, optional per-platform readiness, and a conditional deprecation notice. An entity MAY be 'stable' overall while individual platforms are still 'experimental' or 'draft'.",
          "required": [
            "overall"
          ],
          "properties": {
            "overall": {
              "$ref": "#/$defs/statusValue",
              "description": "Overall lifecycle status of the entity. An editorial judgment of maturity. It need not match the most or least advanced platform status. Tools SHOULD display this as the primary status indicator."
            },
            "description": {
              "type": "string",
              "description": "Optional note explaining what this status represents for the entity — the reasoning behind it, its scope, or any caveats (e.g., 'Stable on web; the API is frozen and changes follow semver. Mobile parity is still in progress.'). Separate from deprecationNotice, which covers only the deprecated case. Tools MAY surface this alongside the status indicator."
            },
            "platforms": {
              "type": "object",
              "description": "Per-platform readiness. Keys are platform identifiers (freeform strings; common values include 'react', 'web-component', 'ios', 'android', 'flutter', 'figma', 'sketch', 'compose'). Values describe the entity's status on that platform.",
              "additionalProperties": {
                "$ref": "#/$defs/platformStatus"
              },
              "minProperties": 1,
              "propertyNames": {
                "pattern": "^[a-z][a-z0-9-]*$"
              }
            },
            "deprecationNotice": {
              "$ref": "#/$defs/deprecationNotice",
              "description": "Required when overall status is 'deprecated'. MUST say what to use instead (non-empty) and give a migration path. Tools SHOULD display this prominently next to the status indicator."
            }
          },
          "if": {
            "properties": {
              "overall": {
                "const": "deprecated"
              }
            }
          },
          "then": {
            "required": [
              "deprecationNotice"
            ]
          },
          "additionalProperties": false
        }
      ]
    },
    "summary": {
      "type": "string",
      "description": "One-line plain-text summary for compact display contexts (list views, search results, hover cards). MUST NOT contain markup.",
      "minLength": 1
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1,
      "description": "Freeform keywords for grouping, search, and cross-referencing.",
      "uniqueItems": true
    },
    "thumbnail": {
      "type": "object",
      "description": "A visual thumbnail for compact display contexts. It is a single image, carried as a URL plus required alt text.",
      "required": [
        "url",
        "alt"
      ],
      "properties": {
        "url": {
          "$ref": "#/$defs/mediaUrl",
          "description": "URL or relative path to the thumbnail image."
        },
        "alt": {
          "$ref": "#/$defs/mediaAlt",
          "description": "Alternative text describing the thumbnail image. MUST be provided for accessibility and MUST NOT be empty."
        }
      },
      "additionalProperties": false
    },
    "keyboardInteraction": {
      "type": "object",
      "description": "A keyboard interaction spec documenting what happens when a specific key or key combination is pressed.",
      "required": [
        "key",
        "action"
      ],
      "properties": {
        "key": {
          "type": "string",
          "description": "The key or key combination (e.g., 'Enter', 'Space', 'Tab', 'Shift+Tab', 'Escape', 'Arrow Down', 'Home', 'End')."
        },
        "action": {
          "type": "string",
          "description": "What happens when this key is pressed (e.g., 'Activates the button', 'Moves focus to the next item in the list', 'Closes the dialog and returns focus to the trigger')."
        },
        "context": {
          "type": "string",
          "description": "Preconditions for this interaction (e.g., 'When the menu is open', 'When focus is on the trigger element', 'When the component is not disabled'). When omitted, the interaction applies in all contexts."
        }
      },
      "additionalProperties": false
    },
    "ariaAttribute": {
      "type": "object",
      "description": "An ARIA attribute used by the artifact: what it communicates and when to apply it.",
      "required": [
        "attribute",
        "description"
      ],
      "properties": {
        "attribute": {
          "type": "string",
          "description": "The ARIA attribute name (e.g., 'role', 'aria-label', 'aria-expanded', 'aria-disabled', 'aria-live', 'aria-controls')."
        },
        "value": {
          "type": "string",
          "description": "The expected value or value pattern (e.g., 'true | false', 'string', 'button', 'dialog', 'assertive | polite')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this attribute communicates to assistive technology and when to apply it. Include guidance on dynamic value changes when applicable."
        },
        "required": {
          "type": "boolean",
          "default": false,
          "description": "Whether this attribute is always required on the artifact. Defaults to false, meaning the attribute is conditionally required or optional."
        }
      },
      "additionalProperties": false
    },
    "colorContrastEntry": {
      "type": "object",
      "description": "A foreground/background color pairing the artifact uses. The pair is the documentation — which combinations occur, and where. Measured contrast ratios are conformance results, not documentation: verify pairs with a criterion (verification: automated) whose check computes ratios from live token values, so the measurement cannot drift from the palette.",
      "required": [
        "foreground",
        "background"
      ],
      "properties": {
        "foreground": {
          "type": "string",
          "description": "The foreground token name or resolved color value (e.g., 'color-text-primary', '#1a1a1a', 'var(--button-text-color)')."
        },
        "background": {
          "type": "string",
          "description": "The background token name or resolved color value (e.g., 'color-background-default', '#ffffff', 'var(--button-background)')."
        },
        "context": {
          "type": "string",
          "description": "Where this foreground/background combination is used (e.g., 'Label text on primary button background in light mode', 'Secondary text on card surface')."
        }
      },
      "additionalProperties": false
    },
    "accessibility": {
      "type": "object",
      "description": "Specialized, structured accessibility documentation for an artifact. Every field is data: keyboard interactions, ARIA attributes, per-state announcements, per-trigger focus behaviors, color pairings, and per-animation reduced-motion behaviors. Each entry states what the artifact does, verifiably. This block carries no prose rules — general documentation guidance, with rationale and a conformance level, belongs in a `guidelines` block (category 'accessibility'), and the two blocks complement each other on the same artifact.",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "accessibility",
          "description": "Identifies this document block as an accessibility spec."
        },
        "wcagLevel": {
          "type": "string",
          "enum": [
            "A",
            "AA",
            "AAA"
          ],
          "description": "The minimum WCAG conformance level targeted by this artifact. Most design systems target AA. Set to AAA when the artifact exceeds standard requirements."
        },
        "criteria": {
          "type": "array",
          "description": "Testable accessibility success criteria for this artifact (e.g., 'All interactive targets present a hit area of at least 44×44 px'). Each criterion pairs a stable identifier with an objectively verifiable statement. This lets accessibility checks run against the docs and report pass/fail. The structured data below (keyboard interactions, ARIA attributes, contrast pairs) shows how these conditions are met.",
          "items": {
            "$ref": "#/$defs/criterion"
          },
          "minItems": 1
        },
        "keyboardInteractions": {
          "type": "array",
          "description": "Keyboard interaction specs: what happens when each key or key combination is pressed while the artifact has focus.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/keyboardInteraction"
          }
        },
        "ariaAttributes": {
          "type": "array",
          "description": "ARIA attribute docs. Lists every ARIA attribute the artifact uses, what it communicates, and when to apply it.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/ariaAttribute"
          }
        },
        "announcements": {
          "type": "array",
          "description": "Screen reader announcements, one entry per state or situation. Name a specific screen reader on an entry when behavior differs between them.",
          "items": {
            "$ref": "#/$defs/announcement"
          },
          "minItems": 1
        },
        "focusBehaviors": {
          "type": "array",
          "description": "Focus movement, one entry per trigger: how focus enters, moves within, and leaves the artifact — including trapping and restoration.",
          "items": {
            "$ref": "#/$defs/focusBehavior"
          },
          "minItems": 1
        },
        "colorContrast": {
          "type": "array",
          "description": "The foreground/background color pairings this artifact uses, one entry per pairing. Pairs are declarations of intent; measure them with an automated contrast criterion, not by hand.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/colorContrastEntry"
          }
        },
        "reducedMotion": {
          "type": "array",
          "description": "Animations and their prefers-reduced-motion behavior, one entry per animation.",
          "items": {
            "$ref": "#/$defs/reducedMotionEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false,
      "anyOf": [
        {
          "required": [
            "wcagLevel"
          ]
        },
        {
          "required": [
            "criteria"
          ]
        },
        {
          "required": [
            "keyboardInteractions"
          ]
        },
        {
          "required": [
            "ariaAttributes"
          ]
        },
        {
          "required": [
            "announcements"
          ]
        },
        {
          "required": [
            "focusBehaviors"
          ]
        },
        {
          "required": [
            "colorContrast"
          ]
        },
        {
          "required": [
            "reducedMotion"
          ]
        }
      ]
    },
    "announcement": {
      "type": "object",
      "description": "What a screen reader announces in one context or state.",
      "required": [
        "context",
        "announcement"
      ],
      "properties": {
        "context": {
          "type": "string",
          "description": "The state or situation being announced (e.g., 'default', 'disabled via aria-disabled', 'loading', 'validation failure'). Use 'default' for the resting state."
        },
        "announcement": {
          "type": "string",
          "description": "What is announced (e.g., '[label], button, dimmed'). Use bracketed placeholders for content that varies."
        },
        "screenReader": {
          "type": "string",
          "description": "The specific screen reader this announcement applies to (e.g., 'VoiceOver', 'NVDA', 'JAWS'). When omitted, the announcement applies to all screen readers."
        }
      },
      "additionalProperties": false
    },
    "focusBehavior": {
      "type": "object",
      "description": "How focus moves at one moment in the artifact's lifecycle.",
      "required": [
        "trigger",
        "behavior"
      ],
      "properties": {
        "trigger": {
          "type": "string",
          "description": "The moment or event (e.g., 'default', 'open', 'close', 'validation failure', 'disabled'). Use 'default' for normal tab-order participation."
        },
        "behavior": {
          "type": "string",
          "description": "What focus does (e.g., 'Moves to the error summary', 'Returns to the trigger element', 'Participates in the normal tab order')."
        }
      },
      "additionalProperties": false
    },
    "reducedMotionEntry": {
      "type": "object",
      "description": "One animation and its behavior under prefers-reduced-motion.",
      "required": [
        "animation",
        "behavior"
      ],
      "properties": {
        "animation": {
          "type": "string",
          "description": "The animation or transition (e.g., 'loading spinner rotation', 'background-color 100ms transition')."
        },
        "behavior": {
          "type": "string",
          "description": "What happens when reduced motion is preferred (e.g., 'Replaced with a static ellipsis indicator', 'Transition removed; state changes apply instantly')."
        }
      },
      "additionalProperties": false
    },
    "anatomyEntry": {
      "type": "object",
      "description": "A named sub-element of a component's or pattern's visual structure.",
      "required": [
        "identifier",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "Machine-readable identifier for the part (e.g., 'container', 'label', 'icon', 'focus-ring')."
        },
        "name": {
          "type": "string",
          "description": "Human-readable name (e.g., 'Container', 'Label', 'Leading Icon')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this part is, what it does, and any constraints on its content or appearance."
        },
        "required": {
          "type": "boolean",
          "default": false,
          "description": "Whether this part is always present in the rendered output. Defaults to false. When false, the part is conditionally rendered based on props or content."
        },
        "tokens": {
          "$ref": "#/$defs/tokenOverrides",
          "description": "Design tokens applied to this part, as the shared token-override map: keys are token-purpose names, values are documented token identifiers."
        },
        "links": {
          "type": "array",
          "description": "Links to resources for this anatomy part: design tool nodes, source code blocks, documentation sections, or other addressable references.",
          "items": {
            "$ref": "#/$defs/link"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "anatomy": {
      "type": "object",
      "description": "Documents the visual structure of a component or pattern by listing its named sub-elements (parts). Each part can link to the design tokens that style it, tying visual design to token architecture. Optional examples show annotated diagrams of the assembled structure. For components, parts are rendered UI sub-elements (e.g., container, label, icon, focus-ring). For patterns, parts are structural sections of the layout (e.g., image, title, body, primary action).",
      "required": [
        "kind",
        "parts"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "anatomy",
          "description": "Identifies this document block as an anatomy spec."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "Prose overview of the artifact's visual structure: how the parts relate and any notable structural constraints."
        },
        "parts": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/anatomyEntry"
          },
          "minItems": 1,
          "description": "The named sub-elements of the artifact, in visual order (typically outside-in or top-to-bottom). Each part documents an element that can be styled, tested, or referenced independently."
        },
        "examples": {
          "type": "array",
          "description": "Examples of the anatomy, typically annotated diagrams with numbered callouts that map to the parts array. Multiple examples allow different views (e.g., expanded vs. collapsed, with icon vs. without).",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "apiProperty": {
      "type": "object",
      "description": "A configurable property of a component.",
      "required": [
        "identifier",
        "type",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "The property identifier as used in code (e.g., 'variant', 'size', 'disabled', 'onClick')."
        },
        "type": {
          "type": "string",
          "description": "The property's type as it would appear in a type signature (e.g., 'boolean', 'number', 'string', 'ReactNode', 'IconComponent'). For enum properties, use `values` instead of listing members here. When `schema` is present it is authoritative; `type` is its display summary and MUST NOT disagree with it."
        },
        "values": {
          "type": "array",
          "description": "The accepted values for enum-like properties (e.g., ['default', 'primary', 'ghost']). Use this instead of listing members in `type`. When `schema` is present it is authoritative; `values` is its display summary and MUST NOT disagree with it.",
          "items": {
            "type": "string"
          },
          "minItems": 1
        },
        "schema": {
          "type": "object",
          "description": "A JSON Schema (Draft 2020-12) object that defines the property's type in machine-readable form. It follows the same conventions as OpenAPI parameter schemas. When present, `schema` is the authoritative type definition; `type` and `values` are display summaries of it and MUST NOT disagree."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this property controls, how it affects the component, and any constraints or side effects."
        },
        "required": {
          "type": "boolean",
          "default": false,
          "description": "Whether the consumer must provide this property. Defaults to false."
        },
        "defaultValue": {
          "description": "The default value in its native JSON type. Accepts any JSON type: string, number, boolean, object, array, or null."
        },
        "examples": {
          "type": "array",
          "description": "Example values for this property.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        },
        "since": {
          "type": "string",
          "description": "The version in which this property was introduced."
        },
        "deprecated": {
          "type": "boolean",
          "default": false,
          "description": "Whether this property is deprecated. Defaults to false."
        },
        "deprecationNotice": {
          "type": "string",
          "description": "What to use instead, if deprecated."
        }
      },
      "additionalProperties": false
    },
    "apiEvent": {
      "type": "object",
      "description": "An event emitted by a component.",
      "required": [
        "identifier",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "The event identifier (e.g., 'onClick', 'onChange', 'onDismiss')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "When this event fires, what it communicates, and how consumers should respond. Add any behavioral context worth noting in prose (DOM propagation, cancelability, deprecation, and conditions under which it does not fire). This is the single home for event documentation."
        },
        "returns": {
          "type": "string",
          "description": "The event's payload or handler signature (e.g., '(event: MouseEvent) => void', '{ value: string, index: number }'). Emitted events do not return values; this documents what a listener receives."
        },
        "since": {
          "type": "string",
          "description": "The version in which this event was introduced."
        }
      },
      "additionalProperties": false
    },
    "apiSlot": {
      "type": "object",
      "description": "A content insertion point of a component.",
      "required": [
        "identifier",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "The slot identifier. Use 'default' for the unnamed/default slot."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "The slot's purpose and where it renders in the component. Content rules belong in `acceptedContent`, not here."
        },
        "acceptedContent": {
          "$ref": "#/$defs/richText",
          "description": "Allowed content (e.g., 'Plain text or a text node. Do not nest interactive elements.')."
        },
        "since": {
          "type": "string",
          "description": "The version in which this slot was introduced."
        }
      },
      "additionalProperties": false
    },
    "apiCssCustomProperty": {
      "type": "object",
      "description": "A CSS custom property exposed by a component for external styling.",
      "required": [
        "identifier",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "pattern": "^--",
          "description": "The full custom-property identifier including the -- prefix (e.g., '--button-background', '--button-border-radius')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What attribute this property controls."
        },
        "defaultValue": {
          "type": "string",
          "description": "The default value (e.g., 'var(--color-action-primary)', '1rem', 'transparent')."
        },
        "type": {
          "type": "string",
          "description": "The expected CSS value type (e.g., 'color', 'dimension', 'number')."
        },
        "since": {
          "type": "string",
          "description": "The version in which this custom property was introduced."
        }
      },
      "additionalProperties": false
    },
    "apiCssPart": {
      "type": "object",
      "description": "A CSS shadow part exposed by a web component for external styling via ::part() selectors.",
      "required": [
        "identifier",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "The part identifier as used in ::part() selectors."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What element this part exposes and what styling it enables."
        },
        "since": {
          "type": "string",
          "description": "The version in which this part was introduced."
        }
      },
      "additionalProperties": false
    },
    "apiDataAttribute": {
      "type": "object",
      "description": "A data attribute used by a component to reflect state or variant info into the DOM.",
      "required": [
        "identifier",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "pattern": "^data-",
          "description": "The full attribute identifier including the data- prefix (e.g., 'data-state', 'data-variant')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this attribute reflects and how to use it for styling or testing."
        },
        "values": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The set of possible values (e.g., ['default', 'hover', 'active', 'disabled']).",
          "minItems": 1
        },
        "since": {
          "type": "string",
          "description": "The version in which this attribute was introduced."
        }
      },
      "additionalProperties": false
    },
    "apiMethod": {
      "type": "object",
      "description": "A public method on a component instance.",
      "required": [
        "identifier",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "The method identifier (e.g., 'focus', 'reset', 'scrollTo')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this method does, when to call it, and any side effects."
        },
        "parameters": {
          "type": "string",
          "description": "The parameter signature (e.g., '(options?: { preventScroll: boolean }) => void')."
        },
        "returnType": {
          "type": "string",
          "description": "The return value (e.g., 'void', 'Promise<void>', 'HTMLElement | null')."
        },
        "since": {
          "type": "string",
          "description": "The version in which this method was introduced."
        }
      },
      "additionalProperties": false
    },
    "api": {
      "type": "object",
      "description": "Documents the code-level interface of a component on a single platform. Lists configurable properties, emitted events, content slots, CSS hooks, data attributes, and public methods. For multi-platform components (e.g., React + Web Component + Vue), create one API block per platform. Use `platform` to tell them apart.",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "api",
          "description": "Identifies this document block as an API spec."
        },
        "platform": {
          "type": "string",
          "description": "The platform or framework this API describes (e.g., 'react', 'web-component', 'vue', 'angular', 'ios', 'android'). When omitted, the API is taken to be the single/default platform."
        },
        "properties": {
          "type": "array",
          "description": "Configurable properties (props, attributes, inputs) that consumers pass to the component.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/apiProperty"
          }
        },
        "events": {
          "type": "array",
          "description": "Events emitted by the component that consumers can listen to.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/apiEvent"
          }
        },
        "slots": {
          "type": "array",
          "description": "Content insertion points where consumers can project child content.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/apiSlot"
          }
        },
        "cssCustomProperties": {
          "type": "array",
          "description": "CSS custom properties exposed for external styling.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/apiCssCustomProperty"
          }
        },
        "cssParts": {
          "type": "array",
          "description": "CSS shadow parts exposed for external styling via ::part() selectors.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/apiCssPart"
          }
        },
        "dataAttributes": {
          "type": "array",
          "description": "Data attributes that reflect component state or config into the DOM.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/apiDataAttribute"
          }
        },
        "methods": {
          "type": "array",
          "description": "Public methods on the component instance.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/apiMethod"
          }
        }
      },
      "additionalProperties": false,
      "anyOf": [
        {
          "required": [
            "properties"
          ]
        },
        {
          "required": [
            "events"
          ]
        },
        {
          "required": [
            "slots"
          ]
        },
        {
          "required": [
            "cssCustomProperties"
          ]
        },
        {
          "required": [
            "cssParts"
          ]
        },
        {
          "required": [
            "dataAttributes"
          ]
        },
        {
          "required": [
            "methods"
          ]
        }
      ]
    },
    "checklistItem": {
      "type": "object",
      "description": "A single checklist item: one concrete thing to verify, do, or confirm. `label` is the actionable statement an agent works through. `description` adds supporting detail. `level` sets how strictly it must be satisfied. `criterion` points to a testable success criterion that backs the item. `optional` marks an item that can be skipped. An item MUST carry a `label`; all other fields are optional.",
      "required": [
        "label"
      ],
      "properties": {
        "label": {
          "$ref": "#/$defs/richText",
          "description": "The check, written as a concrete, imperative statement an agent can act on and then mark done or not done (e.g., 'Give every icon-only button an aria-label', 'Keep one primary-emphasis button per surface'). MUST be specific enough to confirm. Avoid vague phrasing like 'check accessibility'."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "Optional detail: how to satisfy the item, what to watch for, or why it matters. Keep the actionable instruction in `label` and use this for supporting context."
        },
        "level": {
          "$ref": "#/$defs/conformanceLevel",
          "description": "How strictly this item must be satisfied, named for RFC 2119 requirement levels: 'must' (a hard gate; failing it is a defect), 'should' (follow unless justified), 'should-not', 'must-not'. Agents treat must/must-not items as hard gates. When omitted, the item is a recommended check."
        },
        "criterion": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Optional identifier of a testable success criterion (from a guideline or accessibility block on the same entity) that verifies this item. MUST be lowercase kebab-case. Lets an agent jump from the checklist item to the criterion that defines pass/fail."
        },
        "category": {
          "type": "string",
          "description": "Optional discipline this item belongs to, used to group a long checklist. Standard values match the guideline categories: 'visual-design', 'interaction', 'accessibility', 'content', 'motion', 'development'. Custom values are allowed and SHOULD be lowercase kebab-case."
        },
        "optional": {
          "type": "boolean",
          "default": false,
          "description": "Whether the item can be skipped without failing the checklist. Defaults to false. Tools MAY render optional items distinctly (e.g., an 'optional' label)."
        }
      },
      "additionalProperties": false
    },
    "checklist": {
      "type": "object",
      "description": "An explicit checklist for an artifact: a list of concrete items an agent (or a person) works through when building, reviewing, or integrating it. Each item is a checklistItem with an actionable label and an optional level, detail, and criterion link. Use it to turn an artifact's rules into an explicit pass: 'an accessibility review', 'before you ship', 'integration steps'. A general block kind, accepted on every entity type, and equally at home in `agentDocumentBlocks`.",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "checklist",
          "description": "Identifies this document block as a checklist."
        },
        "title": {
          "type": "string",
          "description": "An optional heading for the checklist (e.g., 'Accessibility review', 'Before you ship', 'Integration checklist'). Use it to name the pass the items belong to."
        },
        "ordered": {
          "type": "boolean",
          "default": false,
          "description": "Whether the items must be worked through in order. Defaults to false (an independent checklist where items can be checked in any order). Set true when each item depends on the previous one. Tools SHOULD render unordered checklists as checkboxes and ordered ones as a numbered sequence."
        },
        "items": {
          "type": "array",
          "description": "The checklist items, in display order. Each item is one concrete thing to verify, do, or confirm. Order is significant for ordered checklists, so tools MUST preserve it.",
          "items": {
            "$ref": "#/$defs/checklistItem"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "contentLabelEntry": {
      "type": "object",
      "description": "A single recommended action label or term in the content dictionary. Each entry sets a standard label, its meaning, usage guidance, and cross-references to related or alternative terms. Entries follow the {verb} + {noun} content formula used by systems like Carbon and Gestalt.",
      "required": [
        "term",
        "definition"
      ],
      "properties": {
        "term": {
          "type": "string",
          "description": "The recommended label text (e.g., 'Add', 'Cancel', 'Delete', 'Save as', 'Learn more'). The canonical form as it should appear in the UI."
        },
        "definition": {
          "$ref": "#/$defs/richText",
          "description": "What this label means: the action it represents. Should be concrete and unambiguous (e.g., for Delete: 'Destroys an existing object so that it no longer exists')."
        },
        "usage": {
          "$ref": "#/$defs/richText",
          "description": "When and how to use this label. Include context, formatting rules, and any caveats (e.g., 'Where appropriate, combine Add with the object: Add user, Add role')."
        },
        "alternatives": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Related terms that serve a different purpose but are often confused with this one. Listed for comparison, to help authors pick the right label. The terms SHOULD match other `term` values in this content block's `labels` array — the block is the dictionary they resolve against, so tools can cross-reference them.",
          "minItems": 1
        },
        "context": {
          "type": "string",
          "description": "Where this label is used in the UI (e.g., 'Buttons in dialogs', 'Navigation links', 'Form submission', 'Toolbar actions'). Tells authors where the label normally appears."
        },
        "examples": {
          "type": "array",
          "description": "Visual or code examples showing the label in context: do/don't pairs, screenshots of correct usage, or code snippets.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "localizationEntry": {
      "type": "object",
      "description": "A single localization or i18n concern. Each entry covers something that affects how content is authored, translated, or rendered across languages and writing systems.",
      "required": [
        "concern",
        "description"
      ],
      "properties": {
        "concern": {
          "type": "string",
          "description": "The localization concern being addressed. Standard values: 'rtl' (right-to-left layout mirroring and bidirectional text), 'text-expansion' (content lengthening during translation), 'pluralization' (plural forms varying across languages), 'date-format' (date and time formatting conventions), 'number-format' (decimal separators, grouping), 'currency' (currency symbol placement and formatting), 'text-direction' (mixed-direction content within a single string), 'icon-direction' (icons that imply directionality), 'truncation' (text overflow behavior across languages), 'sorting' (locale-specific sort order and collation), 'concatenation' (avoiding string concatenation that breaks in other languages). Custom values are allowed and SHOULD be lowercase kebab-case."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "Guidance for handling this localization concern. Should be actionable and specific: say what changes across locales, what the author or developer must do, and what to avoid."
        },
        "examples": {
          "type": "array",
          "description": "Visual or code examples that illustrate the concern: before/after screenshots for RTL, samples of text expansion, or code patterns for pluralization.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "content": {
      "type": "object",
      "description": "Specialized, structured content documentation: a terminology dictionary of recommended labels and per-concern localization entries. Each entry is reference data — a fact about the system's vocabulary or its i18n behavior. This block carries no prose rules: general content guidance, with rationale and a conformance level, belongs in a `guidelines` block (category 'content'), and the two blocks complement each other — a guideline says 'Use sentence case' (a rule); a content entry says 'Add: takes an existing object and uses it in a new context' (a reference).",
      "required": [
        "kind"
      ],
      "anyOf": [
        {
          "required": [
            "labels"
          ]
        },
        {
          "required": [
            "localization"
          ]
        }
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "content",
          "description": "Identifies this document block as a content spec."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "Prose overview of what this block covers: the scope of the label dictionary and the localization entries. Content philosophy and voice-and-tone rules are general guidance and belong in a `guidelines` block (category 'content'), not here."
        },
        "labels": {
          "type": "array",
          "description": "Recommended action labels and terminology. Each entry sets a standard label, its meaning, usage guidance, and cross-references to related terms. Order matters, so tools SHOULD keep it for display. Authors MAY order alphabetically or by frequency of use.",
          "items": {
            "$ref": "#/$defs/contentLabelEntry"
          },
          "minItems": 1
        },
        "localization": {
          "type": "array",
          "description": "Localization and i18n notes. Each entry covers a concern that affects content across languages and writing systems: RTL mirroring, text expansion during translation, pluralization rules, and other i18n concerns. Order matters, so tools SHOULD display entries in this order. Authors SHOULD order by impact or frequency.",
          "items": {
            "$ref": "#/$defs/localizationEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "designValue": {
      "description": "A design value. In a system with a token layer, this MUST be a design token identifier (e.g., 'color-action-primary', 'space-4') so the value stays single-sourced and cannot silently drift from the token system. A raw CSS value (e.g., '#0055b3', '16px', '0.4') is allowed only for systems with no token layer. Do not hardcode a raw value that duplicates a token's value.",
      "type": "string"
    },
    "designProperties": {
      "type": "object",
      "description": "A map of design property names to their values. Keys are component-scoped property names that say what the value controls (e.g., 'background', 'border-color', 'min-height', 'font-size', 'opacity'). Values are design token names or raw CSS values. Token-based systems use values like 'color-action-primary'. Non-token systems use values like '#0055b3' or '16px'. You can use either approach, or mix them.",
      "additionalProperties": {
        "$ref": "#/$defs/designValue"
      },
      "examples": [
        {
          "background": "color-action-primary",
          "text-color": "color-text-on-action",
          "border-color": "transparent",
          "border-width": "0px",
          "border-radius": "radius-medium"
        },
        {
          "background": "#0055b3",
          "text-color": "#ffffff",
          "border-radius": "8px",
          "min-height": "40px"
        },
        {
          "background": "button-primary-bg",
          "text-color": "button-primary-text",
          "border-color": "button-primary-border"
        }
      ],
      "minProperties": 1
    },
    "spacingSpec": {
      "type": "object",
      "description": "Spacing specs for the spatial relationships within and around the component. Separates internal spacing (owned by the component) from external spacing (recommended by the component but controlled by the parent layout). Values are design token names or raw CSS values.",
      "properties": {
        "internal": {
          "type": "object",
          "description": "Padding and internal gaps within the component. Keys are named spatial relationships between anatomy parts or edges (e.g., 'container-horizontal', 'icon-to-label'). Values are token names or raw CSS values (e.g., 'space-4', '8px', '1rem').",
          "additionalProperties": {
            "$ref": "#/$defs/designValue"
          }
        },
        "external": {
          "type": "object",
          "description": "Recommended margins and gaps between this component and siblings or container edges. Keys are named spatial relationships (e.g., 'button-to-button', 'button-group-gap', 'stack-gap'). Values are token names or raw CSS values. External spacing is a recommendation. The parent layout controls the space between components.",
          "additionalProperties": {
            "$ref": "#/$defs/designValue"
          }
        }
      },
      "additionalProperties": false,
      "minProperties": 1
    },
    "sizingSpec": {
      "type": "object",
      "description": "Dimension limits that set the min/max bounds of the component and any aspect ratio requirements. All values are token names or raw CSS values.",
      "properties": {
        "minWidth": {
          "$ref": "#/$defs/designValue",
          "description": "Minimum width as a token name or raw value (e.g., 'size-button-min-width', '64px')."
        },
        "maxWidth": {
          "$ref": "#/$defs/designValue",
          "description": "Maximum width as a token name or raw value (e.g., 'size-dialog-max-width', '600px', 'none')."
        },
        "minHeight": {
          "$ref": "#/$defs/designValue",
          "description": "Minimum height as a token name or raw value (e.g., 'size-button-min-height', '44px')."
        },
        "maxHeight": {
          "$ref": "#/$defs/designValue",
          "description": "Maximum height as a token name or raw value."
        },
        "aspectRatio": {
          "type": "string",
          "description": "Aspect ratio constraint as a ratio string (e.g., '16/9', '1/1', '4/3')."
        }
      },
      "additionalProperties": false,
      "minProperties": 1
    },
    "typographyEntrySpec": {
      "type": "object",
      "description": "Typographic settings for a single text element within the component. Maps CSS typographic properties to token names or raw values. When a composite typeToken is provided, the individual properties document what the token resolves to.",
      "properties": {
        "fontFamily": {
          "$ref": "#/$defs/designValue",
          "description": "Font family as a token name or raw value (e.g., 'font-family-body', 'Inter, sans-serif')."
        },
        "fontSize": {
          "$ref": "#/$defs/designValue",
          "description": "Font size as a token name or raw value (e.g., 'font-size-body', '14px', '0.875rem')."
        },
        "fontWeight": {
          "$ref": "#/$defs/designValue",
          "description": "Font weight as a token name or raw value (e.g., 'font-weight-regular', '400', 'bold')."
        },
        "lineHeight": {
          "$ref": "#/$defs/designValue",
          "description": "Line height as a token name or raw value (e.g., 'line-height-body', '1.5', '20px')."
        },
        "letterSpacing": {
          "$ref": "#/$defs/designValue",
          "description": "Letter spacing as a token name or raw value (e.g., 'letter-spacing-body', '0.01em')."
        },
        "textTransform": {
          "type": "string",
          "description": "Text transform value (e.g., 'uppercase', 'capitalize', 'none')."
        },
        "typeToken": {
          "type": "string",
          "description": "A composite typography token covering multiple typographic properties (e.g., '$body-compact-01', 'type-scale-body-sm'). When provided, individual properties (fontSize, fontWeight, etc.) document what the token resolves to."
        }
      },
      "additionalProperties": false,
      "minProperties": 1
    },
    "typographySpec": {
      "type": "object",
      "description": "Typographic specs for text content within the component. Keys are named text elements matching anatomy parts (e.g., 'label', 'helper-text', 'heading', 'body'). Values are typographyEntrySpec objects.",
      "additionalProperties": {
        "$ref": "#/$defs/typographyEntrySpec"
      },
      "minProperties": 1
    },
    "responsiveEntry": {
      "type": "object",
      "description": "A responsive behavior spec describing how the component adapts at a specific breakpoint or viewport condition.",
      "required": [
        "breakpoint",
        "description"
      ],
      "properties": {
        "breakpoint": {
          "type": "string",
          "description": "The breakpoint name or value at which this behavior applies (e.g., 'small', 'medium', 'large', '768px', 'min-width: 1024px')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "How the component's layout, sizing, or behavior changes at this breakpoint."
        }
      },
      "additionalProperties": false
    },
    "designSpecifications": {
      "type": "object",
      "description": "Documents the baseline measurable visual specs of a component: the default design properties, spacing relationships, dimension limits, typographic settings, and responsive behavior. Per-variant and per-state values are not documented here. They live on the variants and states blocks, where each variant value and each state carries its own token overrides. Values are design token identifiers (required when the system has a token layer) or raw CSS values (token-less systems only).",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "design-specifications",
          "description": "Identifies this document block as a design specifications spec."
        },
        "properties": {
          "$ref": "#/$defs/designProperties",
          "description": "Base design properties for the default config. Keys are property names that say what each value controls (e.g., 'background', 'text-color', 'border-radius'). Values are design token names or raw CSS values."
        },
        "spacing": {
          "$ref": "#/$defs/spacingSpec",
          "description": "Base spacing spec for the default config."
        },
        "sizing": {
          "$ref": "#/$defs/sizingSpec",
          "description": "Base sizing spec for the default config."
        },
        "typography": {
          "$ref": "#/$defs/typographySpec",
          "description": "Base typography spec for the default config."
        },
        "responsive": {
          "type": "array",
          "description": "Responsive behavior describing how the component adapts across breakpoints. Entries are ordered from smallest to largest breakpoint.",
          "items": {
            "$ref": "#/$defs/responsiveEntry"
          },
          "minItems": 1
        }
      },
      "anyOf": [
        {
          "required": [
            "properties"
          ]
        },
        {
          "required": [
            "spacing"
          ]
        },
        {
          "required": [
            "sizing"
          ]
        },
        {
          "required": [
            "typography"
          ]
        },
        {
          "required": [
            "responsive"
          ]
        }
      ],
      "additionalProperties": false
    },
    "generalBranches": {
      "description": "Shared `if`/`then` branches for the general block kinds. A fragment composed into every scoped union via allOf; it carries no `kind` enum of its own (the enclosing union owns that). For a non-general `kind`, every `if` here is false and the fragment passes vacuously.",
      "allOf": [
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "checklist"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/checklist"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "guidelines"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/guidelines"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "useCases"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/useCases"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "accessibility"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/accessibility"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "content"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/content"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "sections"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/sections"
          }
        }
      ]
    },
    "generalDocumentBlock": {
      "description": "A document block kind available on every artifact type. General blocks cover concerns that apply to any artifact, including free-form prose and the agent-oriented checklist. Every scoped union includes these kinds.",
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "enum": [
            "checklist",
            "guidelines",
            "useCases",
            "accessibility",
            "content",
            "sections"
          ]
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/generalBranches"
        }
      ]
    },
    "componentDocumentBlock": {
      "description": "A document block kind accepted on component artifacts. Includes the component-specific kinds (imports, anatomy, API, variants, states, design specs) and every general kind, including checklist.",
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "enum": [
            "checklist",
            "imports",
            "anatomy",
            "api",
            "variants",
            "states",
            "design-specifications",
            "guidelines",
            "useCases",
            "accessibility",
            "content",
            "sections"
          ]
        }
      },
      "allOf": [
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "imports"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/imports"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "anatomy"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/anatomy"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "api"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/api"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "variants"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/variants"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "states"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/states"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "design-specifications"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/designSpecifications"
          }
        },
        {
          "$ref": "#/$defs/generalBranches"
        }
      ]
    },
    "foundationDocumentBlock": {
      "description": "A document block kind accepted on foundation artifacts. Includes the foundation-specific kinds (principles, scale, motion) and every general kind, including checklist.",
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "enum": [
            "checklist",
            "principles",
            "scale",
            "motion",
            "guidelines",
            "useCases",
            "accessibility",
            "content",
            "sections"
          ]
        }
      },
      "allOf": [
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "principles"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/principles"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "scale"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/scale"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "motion"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/motion"
          }
        },
        {
          "$ref": "#/$defs/generalBranches"
        }
      ]
    },
    "patternDocumentBlock": {
      "description": "A document block kind accepted on pattern artifacts. Includes the pattern-specific kind (interactions), shared structural kinds (anatomy, variants, states), and every general kind, including checklist.",
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "enum": [
            "checklist",
            "interactions",
            "anatomy",
            "variants",
            "states",
            "guidelines",
            "useCases",
            "accessibility",
            "content",
            "sections"
          ]
        }
      },
      "allOf": [
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "interactions"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/interactions"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "anatomy"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/anatomy"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "variants"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/variants"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "states"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/states"
          }
        },
        {
          "$ref": "#/$defs/generalBranches"
        }
      ]
    },
    "guideDocumentBlock": {
      "description": "A document block kind accepted on guide artifacts. It includes the guide-only `steps` kind, the reused `imports` kind for setup and install steps, and every general kind (including `checklist` — a contribution or migration guide often ends in a verification pass). One of those is the free-form `sections` block, the backbone of a guide. Guides are made for reading, so their blocks favor prose and steps over the measurable specs (api, anatomy, design specs) that components use.",
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "enum": [
            "steps",
            "imports",
            "checklist",
            "guidelines",
            "useCases",
            "accessibility",
            "content",
            "sections"
          ]
        }
      },
      "allOf": [
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "steps"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/steps"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "imports"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/imports"
          }
        },
        {
          "$ref": "#/$defs/generalBranches"
        }
      ]
    },
    "guidelineEntry": {
      "type": "object",
      "description": "A single piece of usage guidance. Each guideline pairs a statement with a rationale. `level` sets the RFC 2119 enforcement level. `category` groups by discipline. `target` points to a specific anatomy part or content area. `criteria` holds testable success criteria that verify the rule, the test-definition layer of the documentation. `references` cites the external standards (such as WCAG) the rule addresses.",
      "required": [
        "guidance",
        "level"
      ],
      "properties": {
        "guidance": {
          "$ref": "#/$defs/richText",
          "description": "The guidance statement. MUST be concrete and unambiguous. Avoid vague phrases like 'use sparingly' or 'when possible'."
        },
        "rationale": {
          "$ref": "#/$defs/richText",
          "description": "Why this guidance exists. Cite evidence, accessibility standards, or user research when available. MUST NOT restate the guidance."
        },
        "level": {
          "$ref": "#/$defs/conformanceLevel",
          "description": "How strictly the guideline should be followed, named for RFC 2119 requirement levels: 'must' (non-compliance is a defect), 'should' (follow in most cases; exceptions need justification), 'should-not' (avoid unless justified), 'must-not' (violations are defects). Agents treat must/must-not entries as hard constraints when generating or reviewing code."
        },
        "evidence": {
          "type": "string",
          "description": "Quantitative or empirical backing for this guideline: test run data, audit findings, issue references, or cited sources. Keeping evidence separate from rationale lets tools tell test-derived rules apart from conventional guidance. Example: '9/10 agent test runs failed touch-target minimums when height was overridden below 36px.'"
        },
        "category": {
          "type": "string",
          "description": "The discipline this guideline belongs to. Used to group guidelines by concern. Standard values: 'visual-design' (color, typography, spacing, layout), 'interaction' (behavior, animation, transitions, gestures), 'accessibility' (inclusive design, assistive tech, WCAG), 'content' (text, imagery, tone, voice, labeling), 'motion' (animation timing, easing, reduced-motion), 'development' (implementation patterns, performance, code conventions). Custom values are allowed and SHOULD be lowercase kebab-case."
        },
        "target": {
          "type": "string",
          "description": "The anatomy part or content area this guideline applies to (e.g., 'label', 'icon', 'helper-text'). When omitted, the guideline applies to the whole artifact."
        },
        "criteria": {
          "type": "array",
          "description": "Testable success criteria that verify this guideline. Each criterion pairs a stable identifier with an objectively verifiable statement. This lets tooling and agents check implementations against the rule and report pass/fail. Attach criteria only when the success condition is objectively verifiable. Observed results belong in `evidence`.",
          "items": {
            "$ref": "#/$defs/criterion"
          },
          "minItems": 1
        },
        "references": {
          "type": "array",
          "description": "External standards this guideline addresses (e.g., WCAG success criteria, MDN or platform guidelines). Each entry cites a URL with an optional display label.",
          "items": {
            "$ref": "#/$defs/reference"
          },
          "minItems": 1
        },
        "examples": {
          "type": "array",
          "description": "Examples showing encouraged and discouraged approaches.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Freeform keywords that relate guidelines across categories and disciplines (e.g., 'rtl', 'localization', 'validation', 'contrast').",
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "guidelines": {
      "type": "object",
      "description": "Actionable usage rules for an artifact. Each item pairs a guidance statement with a rationale. Categorize by discipline, target a specific anatomy part, and link to external standards like WCAG. Use `guidelines` to answer *how* to use the artifact correctly: concrete do/don't rules, each with a rationale. For *whether* to use it at all, see `useCases`. For the higher-level beliefs behind the rules (on foundations), see `principles`. Guidelines are the home for general documentation guidance in every discipline — including accessibility rules (category 'accessibility') and content rules (category 'content'); the `accessibility` and `content` blocks are reserved for specialized, structured data.",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "items": {
          "type": "array",
          "description": "The guideline rules. Each item is a guidance statement paired with a rationale. Order matters, so tools SHOULD display guidelines in this order. Authors SHOULD order by importance or group by category.",
          "items": {
            "$ref": "#/$defs/guidelineEntry"
          },
          "minItems": 1
        },
        "kind": {
          "type": "string",
          "const": "guidelines",
          "description": "Identifies this document block as a guidelines spec."
        }
      },
      "additionalProperties": false
    },
    "importEntry": {
      "type": "object",
      "description": "A single platform-specific import statement for a component. Pairs the platform identifier with the code needed to import and use the component.",
      "required": [
        "code"
      ],
      "properties": {
        "platform": {
          "type": "string",
          "description": "The platform or framework this import applies to (e.g., 'react', 'vue', 'angular', 'svelte', 'web-component', 'ios', 'android'). Use the same platform identifiers as the API document block. When omitted, the import is taken to apply to the single/default platform."
        },
        "package": {
          "type": "string",
          "description": "The package or module to install (e.g., '@acme/ui', '@acme/ui-vue', 'AcmeUI'). Include the full package name as it appears in the package manager."
        },
        "code": {
          "type": "string",
          "description": "The import statement or code snippet. SHOULD be copy-pasteable and ready to use. For multi-line snippets (e.g., install + import), use newlines."
        },
        "language": {
          "type": "string",
          "description": "The language or format of the code (e.g., 'tsx', 'js', 'vue', 'swift', 'kotlin'). Omit when obvious from the platform."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "Additional context about this import: when to use this path over others, version requirements, or setup prerequisites."
        },
        "since": {
          "type": "string",
          "description": "The version in which this import path became available."
        }
      },
      "additionalProperties": false
    },
    "imports": {
      "type": "object",
      "description": "Documents how to import a component across platforms. Each item is a platform-specific import entry with the code needed to use the component. For components available on multiple platforms, include one entry per platform. Ordering is significant, so list the primary/recommended platform first.",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "imports",
          "description": "Identifies this document block as an imports spec."
        },
        "items": {
          "type": "array",
          "description": "Platform-specific import entries. Each item documents how to import the component on a specific platform or framework. List the primary/recommended platform first.",
          "items": {
            "$ref": "#/$defs/importEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "interactionEntry": {
      "type": "object",
      "description": "A single step in an interaction flow. Each interaction describes a trigger (what starts the step), a description (what happens), the components involved, and optional examples.",
      "required": [
        "description"
      ],
      "properties": {
        "trigger": {
          "type": "string",
          "description": "What starts this interaction step: a user action, system event, or condition (e.g., 'User submits the form by activating the submit button', 'API returns a 422 response'). When omitted, the step continues the previous step's flow without a new trigger."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What happens during this step: the system's response, visual changes, state transitions, and user-facing feedback. CommonMark supported."
        },
        "components": {
          "type": "array",
          "description": "The entities involved in this step, as entity references. Each `identifier` MUST match a component documented in the system; the optional `role` says what the component does in this step. Creates a traversable cross-reference between the pattern's interaction flow and individual component docs.",
          "items": {
            "$ref": "#/$defs/entityRef"
          },
          "minItems": 1
        },
        "examples": {
          "type": "array",
          "description": "Visual or interactive examples for this step: screenshots of the resulting state, screen recordings of the transition, or code snippets showing the implementation.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "interactions": {
      "type": "object",
      "description": "Documents the interaction flow of a pattern as an ordered sequence of steps. Each item describes a single step: what triggers it, what the system does in response, which components are involved, and optional examples. Item ordering is critical. It represents the flow of the user journey over time. Authors SHOULD include steps for the initial trigger, the system's response, recovery or correction steps, and completion. Multiple interactions blocks on the same pattern can document alternative flows (e.g., a happy path and an error path).",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "interactions",
          "description": "Identifies this document block as an interactions spec."
        },
        "items": {
          "type": "array",
          "description": "The interaction steps in time order. Each item documents a single step with its trigger, description, involved components, and optional examples. Ordering is significant, so tools SHOULD keep and display steps in this order to show the sequence over time.",
          "items": {
            "$ref": "#/$defs/interactionEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "motionDuration": {
      "type": "object",
      "description": "A recommended duration range for an easing curve. Duration values are strings: a time value (e.g., '200ms', '0.3s') or a token reference (e.g., 'duration-quick'). When both min and max are provided, they define a range. When only one is provided, it serves as a single recommendation. When neither is provided, the description field MUST explain the guidance — an empty duration object carries no information, so at least one of min, max, or description is required.",
      "anyOf": [
        {
          "required": [
            "min"
          ]
        },
        {
          "required": [
            "max"
          ]
        },
        {
          "required": [
            "description"
          ]
        }
      ],
      "properties": {
        "min": {
          "type": "string",
          "description": "Minimum recommended duration as a time value or token reference (e.g., '200ms', '0.3s', 'duration-quick'). When omitted, there is no minimum recommendation."
        },
        "max": {
          "type": "string",
          "description": "Maximum recommended duration as a time value or token reference (e.g., '500ms', '0.5s', 'duration-slow'). When omitted, there is no maximum recommendation."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "Freeform duration guidance. Use it when the recommendation cannot be expressed as a min/max range (e.g., 'Depends on element size: use shorter durations for small elements and longer durations for full-screen transitions'). It also helps for extra notes when min/max are provided."
        }
      },
      "additionalProperties": false
    },
    "motionEntry": {
      "type": "object",
      "description": "A single named easing in the motion system. Documents a cubic-bezier timing function with its recommended usage and duration. The `function` property uses the same four-number array format as the W3C Design Tokens Format Module cubicBezier type: [P1x, P1y, P2x, P2y] where P1x and P2x are in [0, 1] and P1y and P2y can be any real number.",
      "required": [
        "identifier",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "Machine-readable identifier of the easing (e.g., 'expressive', 'enter', 'exit', 'lateral', 'bounce', 'linear'). Used for cross-referencing and tooling."
        },
        "name": {
          "type": "string",
          "description": "Human-readable name of the easing (e.g., 'Expressive Ease', 'Enter Ease', 'Exit Ease')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this easing is for, when to use it, and how it affects the user experience. Explain the visual character of the curve and the types of transitions it suits."
        },
        "function": {
          "type": "array",
          "description": "Cubic-bezier control points as a four-number array [P1x, P1y, P2x, P2y], aligned with the W3C Design Tokens Format Module cubicBezier type (§8.6). P1x and P2x MUST be in [0, 1]. P1y and P2y can be any real number (values outside [0, 1] produce bounce/overshoot effects). Maps to CSS cubic-bezier() and DTCG cubicBezier token values. When omitted, the easing is described in the description field or points to a token via `token`.",
          "minItems": 4,
          "maxItems": 4,
          "prefixItems": [
            {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "description": "P1x — MUST be in [0, 1]."
            },
            {
              "type": "number",
              "description": "P1y — any real number."
            },
            {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "description": "P2x — MUST be in [0, 1]."
            },
            {
              "type": "number",
              "description": "P2y — any real number."
            }
          ]
        },
        "token": {
          "type": "string",
          "description": "Reference to a DTCG cubicBezier or transition token that defines this easing (e.g., 'motion-ease-expressive', 'transition-enter'). When both `function` and `token` are present, `function` is the display value and `token` is the authoritative source."
        },
        "duration": {
          "$ref": "#/$defs/motionDuration",
          "description": "Recommended duration range when using this easing. Different easings suit different durations. Enter transitions are typically longer (300–500ms) to draw attention, while exit transitions are shorter (200–300ms) to feel responsive."
        },
        "usage": {
          "type": "string",
          "description": "A short, comma-separated list of usage contexts (e.g., 'entering elements', 'exiting elements', 'lateral navigation', 'micro-interactions'). Gives a scannable summary. Use description for detailed guidance."
        },
        "examples": {
          "type": "array",
          "description": "Visual or interactive examples showing this easing in action: screen recordings, interactive curve visualizations, or CSS code snippets.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "motion": {
      "type": "object",
      "description": "Documents the motion system for a foundation: named easing curves, timing functions, recommended durations, and usage guidance. Each item defines an easing that designers and engineers select when adding animation or transitions. Ordering is significant, so tools SHOULD display easings in this order. A typical motion block lists easings from most common to most specialized. It is scoped to foundation entities. It complements the scale block (for duration token progressions) and `guidelines` blocks (for motion rules like reduced-motion handling).",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "motion",
          "description": "Identifies this document block as a motion spec."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "An overview of the motion system: the philosophy behind easing choices, how duration and easing interact, and system-wide motion principles not covered by individual easing entries."
        },
        "items": {
          "type": "array",
          "description": "Named easing definitions in the motion system. Each item documents a single easing curve with its cubic-bezier function, recommended duration, and usage guidance. Ordering is significant, so tools SHOULD display easings in this order.",
          "items": {
            "$ref": "#/$defs/motionEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "principleEntry": {
      "type": "object",
      "description": "A single guiding principle. Each principle has a short title and a description that explains what it means in practice and how it guides decisions. Principles are high-level. They set direction rather than prescribe specific actions. For specific, actionable rules, use a `guidelines` block instead. For whether to use an artifact at all, use a `useCases` block.",
      "required": [
        "title",
        "description"
      ],
      "properties": {
        "title": {
          "type": "string",
          "description": "A short, memorable name for the principle (e.g., 'Functional first', 'Accessible by default', 'Use the scale', 'Density over decoration'). Should be concise enough to serve as a heading or reference label."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this principle means and how it guides decisions. Explain the belief or constraint in concrete terms, not just restate the title. Good descriptions answer: 'What does this look like in practice?' and 'How would I use this to resolve a disagreement?'"
        }
      },
      "additionalProperties": false
    },
    "principles": {
      "type": "object",
      "description": "Documents guiding principles for a foundation or design domain. Use `principles` to answer *what the system believes* and which trade-offs it accepts: the philosophy that shapes a domain's decisions. Principles sit *above* `guidelines`. They set direction, while `guidelines` entries encode the specific rules. A spacing foundation might have 'Use the scale' and 'Density over decoration'. A color foundation might have 'Functional first' and 'Accessible by default'.",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "principles",
          "description": "Identifies this document block as a principles spec."
        },
        "items": {
          "type": "array",
          "description": "The guiding principles. Each item documents a single principle with a title and description. Ordering is significant, so tools SHOULD display principles in this order. Authors SHOULD order by importance or by the natural progression of the design philosophy.",
          "items": {
            "$ref": "#/$defs/principleEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "scaleStep": {
      "type": "object",
      "description": "One step in the scale. Carries a `token` (when the step is token-backed), a literal `value` (when it is not — pre-tokenization systems, or scales like column counts that have no token layer), or both. At least one of `token` or `value` is required. Do not fabricate token identifiers to satisfy validation — a value-only step is the honest form.",
      "anyOf": [
        {
          "required": [
            "token"
          ]
        },
        {
          "required": [
            "value"
          ]
        }
      ],
      "properties": {
        "token": {
          "type": "string",
          "description": "The identifier of the token that supplies this step's value. MUST match the `identifier` of a token defined elsewhere in the system. Omit for steps that are not token-backed and use `value` instead."
        },
        "label": {
          "type": "string",
          "description": "A human-readable label for this step (e.g., 'sm', 'md', 'lg', '2xl', 'body', 'heading-1'). Labels give a shorthand vocabulary that designers and engineers use to talk about scale positions without naming raw values."
        },
        "value": {
          "type": "string",
          "description": "The resolved value for display (e.g., '16px', '1rem', '#1a1a1a', '400'). This is a docs convenience. The authoritative value lives in the referenced token. Tools MAY fill this in from the token catalog."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "Usage notes for this step: when to use it, what it pairs with, and any limits (e.g., 'The base unit. Use for default internal component padding.', 'Reserved for page-level hero sections only.')."
        }
      },
      "additionalProperties": false
    },
    "scale": {
      "type": "object",
      "description": "Documents an ordered scale: a deliberate progression of values that limits design decisions to a harmonious, consistent set. Scales are the main mechanism for spacing systems, type hierarchies, elevation levels, and other graduated visual attributes. The steps array runs from smallest/lowest to largest/highest. Tools SHOULD keep this order for display.",
      "required": [
        "kind",
        "identifier",
        "description",
        "steps"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "scale",
          "description": "Identifies this document block as a scale spec."
        },
        "identifier": {
          "type": "string",
          "description": "Machine-readable identifier of the scale (e.g., 'spacing-scale', 'type-scale', 'elevation-scale'). Used for cross-referencing and tooling."
        },
        "name": {
          "type": "string",
          "description": "Human-readable name of the scale (e.g., 'Spacing Scale', 'Type Scale', 'Elevation Levels')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this scale is, how it was built (e.g., geometric progression, modular scale), and the rules for using it. Document the scale's rationale and limits: why these values were chosen and when deviation is appropriate."
        },
        "steps": {
          "type": "array",
          "description": "The ordered steps in the scale, from smallest/lowest to largest/highest. Ordering is significant, so tools SHOULD keep it for display. Consumers SHOULD treat adjacent steps as the natural next-smaller and next-larger options when adjusting a value.",
          "items": {
            "$ref": "#/$defs/scaleStep"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "sectionEntry": {
      "type": "object",
      "description": "A single titled section of prose. Pairs a heading with a rich-text body, optional illustrative examples, optional related links, and optional nested sub-sections. Sub-sections let a guide build a heading hierarchy (h2 → h3 → h4) without inventing one block per level. A section MUST carry a `title`. Everything else is optional, so a section can be a bare heading that only groups its children.",
      "required": [
        "title"
      ],
      "properties": {
        "title": {
          "type": "string",
          "description": "The section heading (e.g., 'Installation', 'Core concepts', 'Why semantic tokens'). Concise enough to render as a heading and to anchor a deep link."
        },
        "anchor": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "A stable, URL-safe fragment identifier for deep-linking to this section (e.g., 'installation', 'core-concepts'). MUST be lowercase kebab-case and unique within the parent block. When omitted, tools MAY derive an anchor from the title."
        },
        "body": {
          "$ref": "#/$defs/richText",
          "description": "The prose content of the section. Supports markdown by default. May be omitted when the section exists only to group sub-sections under a heading."
        },
        "examples": {
          "type": "array",
          "description": "Illustrative examples for this section: code snippets, images, videos, or live URLs. Useful for showing a concept in practice within explanatory prose.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        },
        "links": {
          "type": "array",
          "description": "Related links surfaced with this section: 'see also' references, source material, or pointers to other entities and external resources.",
          "items": {
            "$ref": "#/$defs/link"
          },
          "minItems": 1
        },
        "sections": {
          "type": "array",
          "description": "Nested sub-sections beneath this one. Each item is itself a sectionEntry, allowing an arbitrarily deep heading hierarchy. Ordering is significant, so tools SHOULD render sub-sections in this order.",
          "items": {
            "$ref": "#/$defs/sectionEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "sections": {
      "type": "object",
      "description": "A block of narrative documentation made up of one or more titled sections. Use it for the reading-oriented content of a guide that the structured block kinds do not capture: overviews, concepts, rationale, and FAQs. Each item is a sectionEntry with a heading, prose body, optional examples and links, and optional nested sub-sections.",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "sections",
          "description": "Identifies this document block as narrative sections."
        },
        "items": {
          "type": "array",
          "description": "The sections, in display order. Each item is a titled section with optional body, examples, links, and sub-sections. Ordering is significant, so tools SHOULD keep it.",
          "items": {
            "$ref": "#/$defs/sectionEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "stateEntry": {
      "type": "object",
      "description": "A single interactive state of a component. States are transient or persistent visual or behavioral changes triggered by user interaction, system events, or data conditions.",
      "required": [
        "identifier",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "Machine-readable state identifier (e.g., 'default', 'hover', 'focus', 'active', 'disabled', 'loading', 'selected', 'error', 'read-only')."
        },
        "name": {
          "type": "string",
          "description": "Human-readable name (e.g., 'Default', 'Hover', 'Focus', 'Active / Pressed', 'Disabled', 'Loading')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What triggers this state, how appearance and behavior change, and any constraints or side effects."
        },
        "tokens": {
          "$ref": "#/$defs/tokenOverrides",
          "description": "Design token overrides applied when this state is active, as the shared token-override map: keys are token-purpose names, values are documented token identifiers. List only tokens that change from the default state. Each state carries its own overrides here; on components, baseline (default-state) values live on the design-specifications block (patterns have no such block — their anatomy carries the baseline)."
        },
        "rationale": {
          "$ref": "#/$defs/richText",
          "description": "Why this state exists — the user need or design rationale it addresses."
        },
        "examples": {
          "type": "array",
          "description": "Examples showing the component in this state.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "states": {
      "type": "object",
      "description": "Documents all interactive states of a component or pattern. Each item describes a single state: what triggers it, how appearance and behavior change, which tokens are overridden, and how it looks. List states in a meaningful order. A typical order is default first, then hover, focus, active, followed by disabled, loading, and other special states. Ownership rule: a state is a runtime condition entered through interaction or system events (hover, focus, disabled, loading); a configuration the consumer chooses up front is a variant — document it in the variants block, not here.",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "states",
          "description": "Identifies this document block as a states spec."
        },
        "items": {
          "type": "array",
          "description": "The interactive states. Each item documents a single state with its trigger, visual changes, token overrides, and optional examples. Ordering is significant, so tools SHOULD display states in this order.",
          "items": {
            "$ref": "#/$defs/stateEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "stepEntry": {
      "type": "object",
      "description": "A single step in a procedure. Pairs a short title with an instruction that describes what to do. Examples carry the code, command, or screenshot for the step. `expectedResult` tells the reader what they should see once the step succeeds, which is valuable for tutorials. `optional` marks a step that can be skipped. A step MUST carry a `title`. All other fields are optional.",
      "required": [
        "title"
      ],
      "properties": {
        "title": {
          "type": "string",
          "description": "A short label for the step (e.g., 'Install the package', 'Wrap your app in the provider', 'Run the validator'). Concise enough to serve as a numbered list item or heading."
        },
        "instruction": {
          "$ref": "#/$defs/richText",
          "description": "What the reader should do in this step. Supports markdown by default. MUST be concrete and actionable: describe the action, not the goal."
        },
        "examples": {
          "type": "array",
          "description": "Illustrative material for the step: a code snippet, a terminal command, a screenshot, or a live URL. Most procedural steps carry a single code example.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        },
        "expectedResult": {
          "$ref": "#/$defs/richText",
          "description": "What the reader should see once the step succeeds (e.g., 'The dev server starts on port 3000', 'A themed button renders'). Lets readers confirm progress and spot problems on their own. Most useful in tutorials and getting-started guides."
        },
        "optional": {
          "type": "boolean",
          "default": false,
          "description": "Whether the step can be skipped without breaking the procedure. Defaults to false. Tools MAY render optional steps distinctly (e.g., an 'optional' label)."
        }
      },
      "additionalProperties": false
    },
    "steps": {
      "type": "object",
      "description": "A procedure made up of an ordered or unordered sequence of steps. Use it for any task a reader works through start to finish: installation, a first-component tutorial, a contribution checklist, or a migration. Each item is a stepEntry with a title, instruction, optional examples, and an optional expected result.",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "steps",
          "description": "Identifies this block as a procedure."
        },
        "title": {
          "type": "string",
          "description": "An optional heading for the whole procedure (e.g., 'Installation', 'Build your first component', 'Submitting a contribution')."
        },
        "ordered": {
          "type": "boolean",
          "default": true,
          "description": "Whether the steps must be followed in order. Defaults to true (a sequential procedure). Set false for an unordered checklist where steps are independent. Tools SHOULD render ordered procedures as numbered lists and unordered ones as a checklist or bulleted list."
        },
        "items": {
          "type": "array",
          "description": "The steps, in display order. Each item is a single step with a title and optional instruction, examples, and expected result. Ordering is significant, so tools MUST preserve it for ordered procedures.",
          "items": {
            "$ref": "#/$defs/stepEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "variantValue": {
      "type": "object",
      "description": "A single option within an enum variant dimension.",
      "required": [
        "identifier",
        "description"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "Machine-readable value identifier (e.g., 'sm', 'md', 'lg', 'primary', 'secondary')."
        },
        "name": {
          "type": "string",
          "description": "Human-readable name (e.g., 'Small', 'Medium', 'Large', 'Primary', 'Secondary')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this value looks like, when to use it, and how it differs from other values in this dimension."
        },
        "rationale": {
          "$ref": "#/$defs/richText",
          "description": "Why this value exists: the user need or design rationale it addresses (e.g., 'High emphasis directs the user to the single most important action on the surface'). For comparative when-to-use guidance across values, use the entity's useCases block instead."
        },
        "examples": {
          "type": "array",
          "description": "Examples showing this variant value in isolation or in context.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        },
        "tokens": {
          "$ref": "#/$defs/tokenOverrides",
          "description": "Design token overrides applied when this value is selected, as the shared token-override map: keys are token-purpose names, values are documented token identifiers."
        }
      },
      "additionalProperties": false
    },
    "flagVariant": {
      "type": "object",
      "description": "A boolean toggle variant that is either on or off. Use it for binary capabilities such as 'full-width', 'full-width', 'icon-only', or 'icon-only'. Flag variants have no values array. The component either has the flag active or not.",
      "required": [
        "kind",
        "identifier",
        "description"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "flag",
          "description": "Identifies this variant as a boolean flag."
        },
        "identifier": {
          "type": "string",
          "description": "Machine-readable identifier of the flag (e.g., 'disabled', 'full-width', 'icon-only', 'loading')."
        },
        "name": {
          "type": "string",
          "description": "Human-readable name of the flag (e.g., 'Disabled', 'Full Width', 'Icon Only', 'Loading')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this flag controls and how it affects the component's appearance or behavior when active."
        },
        "rationale": {
          "$ref": "#/$defs/richText",
          "description": "Why this flag exists: the user need or design rationale it addresses (e.g., 'Prevents user interaction during form submission to avoid duplicate requests')."
        },
        "examples": {
          "type": "array",
          "description": "Examples showing the component with this flag active.",
          "items": {
            "$ref": "#/$defs/example"
          },
          "minItems": 1
        },
        "tokens": {
          "$ref": "#/$defs/tokenOverrides",
          "description": "Design token overrides applied when this flag is active, as the shared token-override map: keys are token-purpose names, values are documented token identifiers."
        }
      },
      "additionalProperties": false
    },
    "enumVariant": {
      "type": "object",
      "description": "An enumerated variant with multiple mutually exclusive values. Use it for dimensions where the user picks one option from a set, such as 'size' (sm | md | lg), 'emphasis' (primary | secondary | ghost), or 'shape' (rounded | pill | square).",
      "required": [
        "kind",
        "identifier",
        "description",
        "values"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "enum",
          "description": "Identifies this variant as an enumerated set of values."
        },
        "identifier": {
          "type": "string",
          "description": "Machine-readable identifier of the variant dimension (e.g., 'size', 'emphasis', 'shape'). This is the axis name, not a value within it."
        },
        "name": {
          "type": "string",
          "description": "Human-readable name of the variant dimension (e.g., 'Size', 'Emphasis', 'Shape')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this dimension of variation controls and how its values affect the component's appearance or behavior."
        },
        "values": {
          "type": "array",
          "description": "The possible values for this variant. A single entry is valid — real systems carry single-value dimensions mid-migration (a new dimension introduced one value at a time). Ordering is significant, so tools SHOULD keep it for display.",
          "items": {
            "$ref": "#/$defs/variantValue"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "variants": {
      "type": "object",
      "description": "Documents all dimensions of visual or behavioral variation on a component or pattern. Each item is either a flag variant (a boolean toggle like 'disabled' or 'full-width') or an enum variant (a set of mutually exclusive options like size: sm | md | lg). Multiple items document orthogonal dimensions that combine independently. Ownership rule: a variant is a configuration the consumer chooses via the API or design surface (e.g., 'size', 'emphasis', 'full-width', 'icon-only'); a runtime condition entered through interaction or system events (hover, disabled, loading) is a state — document it in the states block, not here.",
      "required": [
        "kind",
        "items"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "variants",
          "description": "Identifies this document block as a variants spec."
        },
        "items": {
          "type": "array",
          "description": "The variant dimensions. Each item is either a flag (boolean toggle) or an enum (set of values). Ordering is significant, so tools SHOULD display dimensions in this order.",
          "items": {
            "type": "object",
            "description": "One variant dimension, discriminated by `kind`: 'flag' selects the flagVariant schema, 'enum' selects the enumVariant schema. Discriminated `if`/`then` branches (rather than a bare oneOf) keep validation errors scoped to the one schema the `kind` selects.",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "enum": [
                  "flag",
                  "enum"
                ]
              }
            },
            "allOf": [
              {
                "if": {
                  "required": [
                    "kind"
                  ],
                  "properties": {
                    "kind": {
                      "const": "flag"
                    }
                  }
                },
                "then": {
                  "$ref": "#/$defs/flagVariant"
                }
              },
              {
                "if": {
                  "required": [
                    "kind"
                  ],
                  "properties": {
                    "kind": {
                      "const": "enum"
                    }
                  }
                },
                "then": {
                  "$ref": "#/$defs/enumVariant"
                }
              }
            ]
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "chunk": {
      "type": "object",
      "description": "A pre-composed block of code that captures a design system pattern. Chunks give consumers a working starting point assembled from system components — for example, a layout, a settings form, or a confirmation dialog. The top level holds its identity (`identifier`, `name`, optional `description`), the `code` block that consumers copy, and `documentBlocks`/`agentDocumentBlocks` carrying the general block kinds (guidelines, useCases, accessibility, content, sections, checklist) — so a chunk can hold agent-only rules like any other entity. Chunks are intentionally simple: they document a code composition, not the full anatomy/API surface a component carries. The top-level `guidelines` and `useCases` arrays are DEPRECATED authoring shorthand — prefer the equivalent blocks in `documentBlocks`.",
      "required": [
        "kind",
        "identifier",
        "name",
        "code"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "chunk",
          "description": "Identifies this entity as a chunk."
        },
        "identifier": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Machine-readable identifier for the chunk (e.g., 'search-bar', 'settings-form', 'confirmation-dialog'). MUST be lowercase kebab-case and unique within its entity group."
        },
        "name": {
          "type": "string",
          "description": "Display name shown in docs (e.g., 'Search bar', 'Settings form', 'Confirmation dialog')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this chunk is, the pattern it captures, and which components it composes. CommonMark supported."
        },
        "code": {
          "description": "The pre-composed block of code consumers copy to use the chunk. Provide it in exactly one of two forms: inline (`code` + `language`) when the source travels with the document, or referenced (`src` + `language`) when it lives in an external source file. The referenced form keeps long sources readable and lets a chunk share one canonical file with a live app. Consumers resolve `src` at read time — no build step inlines it.",
          "oneOf": [
            {
              "type": "object",
              "title": "Inline code",
              "description": "The source travels inside the document as a string.",
              "required": [
                "code",
                "language"
              ],
              "properties": {
                "code": {
                  "type": "string",
                  "description": "The source code of the chunk — the block consumers copy and adapt."
                },
                "language": {
                  "type": "string",
                  "description": "The programming language or syntax of the code (e.g., 'jsx', 'tsx', 'html', 'css', 'vue', 'svelte')."
                }
              },
              "additionalProperties": false
            },
            {
              "type": "object",
              "title": "Referenced code",
              "description": "The source lives in an external file the consumer resolves at read time.",
              "required": [
                "src",
                "language"
              ],
              "properties": {
                "src": {
                  "type": "string",
                  "pattern": "^(?![a-zA-Z][a-zA-Z0-9+.\\-]*:)(?!//)(?!/)",
                  "description": "A relative path to the source file that holds the chunk's code (e.g., './chunks/confirmation-dialog.tsx'). MUST be relative — the pattern rejects absolute paths, protocol-relative references, and scheme-bearing URIs (http:, file:, …). Resolvers SHOULD gate any remote dereferencing behind an allow-list, following the same rules a `$ref` fileRef obeys. The file is read verbatim — no build step inlines it into the document.",
                  "minLength": 1
                },
                "language": {
                  "type": "string",
                  "description": "The programming language or syntax of the referenced file (e.g., 'jsx', 'tsx', 'html', 'css', 'vue', 'svelte')."
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "documentBlocks": {
          "type": "array",
          "description": "Structured docs for this chunk, as an ordered array of general document blocks (guidelines, useCases, accessibility, content, sections, checklist). Chunks accept the general kinds only — they document a code composition, not the anatomy/API surface a component carries.",
          "items": {
            "$ref": "#/$defs/generalDocumentBlock"
          },
          "minItems": 1
        },
        "agentDocumentBlocks": {
          "type": "array",
          "description": "Structured docs for AI agents consuming this chunk, using the same general block kinds as `documentBlocks`. Content here is agent-only: hard MUST/MUST-NOT rules, verification checklists, and adaptation constraints an agent needs when copying the chunk into an application. Human-facing guidance belongs in `documentBlocks`.",
          "items": {
            "$ref": "#/$defs/generalDocumentBlock"
          },
          "minItems": 1
        },
        "guidelines": {
          "type": "array",
          "description": "DEPRECATED authoring shorthand — prefer a `guidelines` block inside `documentBlocks`. Actionable rules for using and adapting the chunk. Each item pairs a guidance statement with a rationale and an RFC 2119 enforcement level. Reuses the shared guideline-entry shape so chunk rules read the same as guidelines elsewhere in the system. When both this shorthand and an equivalent block in `documentBlocks` are present, tools MUST treat the `documentBlocks` block as authoritative.",
          "items": {
            "$ref": "#/$defs/guidelineEntry"
          },
          "minItems": 1,
          "deprecated": true
        },
        "useCases": {
          "type": "array",
          "description": "DEPRECATED authoring shorthand — prefer a `useCases` block inside `documentBlocks`. Scenario-driven use cases describing when this chunk is — or is not — the right choice. Each entry is marked 'recommended' or 'discouraged'. Reuses the shared use-case shape. Authors SHOULD lead with recommended scenarios. When both this shorthand and an equivalent block in `documentBlocks` are present, tools MUST treat the `documentBlocks` block as authoritative.",
          "items": {
            "$ref": "#/$defs/useCase"
          },
          "minItems": 1,
          "deprecated": true
        },
        "relationships": {
          "$ref": "#/$defs/relationships",
          "description": "Typed dependency edges from this chunk to other entities — chiefly 'composes' the components it is built from (mark `required` true for essential ones). This is the graph agents read for impact and alternatives. Inverse edges are derived by tooling."
        },
        "metadata": {
          "$ref": "#/$defs/entityMetadata",
          "description": "Optional metadata: the shared entityMetadata fields (see metadata/metadata.schema.json). Declare the components this chunk composes as `relationships` edges, not links."
        },
        "$extensions": {
          "$ref": "#/$defs/extensions"
        }
      },
      "additionalProperties": false
    },
    "component": {
      "type": "object",
      "description": "A single reusable UI component. The top level holds its identity: `identifier`, `name`, and `description`. The `metadata` object holds the shared entityMetadata fields (see metadata/metadata.schema.json). The `documentBlocks` array holds structured docs as typed entries: imports, anatomy, api, variants, states, design-specifications, plus the general kinds.",
      "required": [
        "kind",
        "identifier",
        "name"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "component",
          "description": "Identifies this entity as a component."
        },
        "identifier": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Machine-readable identifier (e.g., 'button', 'dialog', 'form-field', 'data-table'). MUST be unique within its entity group."
        },
        "name": {
          "type": "string",
          "description": "Display name shown in docs (e.g., 'Button', 'Dialog', 'Form Field', 'Data Table')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this component is, what it does, and its role in the design system. CommonMark supported."
        },
        "metadata": {
          "$ref": "#/$defs/entityMetadata",
          "description": "Optional metadata: the shared entityMetadata fields (see metadata/metadata.schema.json)."
        },
        "relationships": {
          "$ref": "#/$defs/relationships",
          "description": "Typed dependency edges from this component to other entities — e.g. 'depends-on' a token, 'composes' another component, 'alternative-to' a sibling. The structural graph agents read for impact and alternatives. Inverse edges are derived by tooling. Use the `links` metadata field for external resources."
        },
        "documentBlocks": {
          "type": "array",
          "description": "All structured docs for this component, as an ordered array of typed document block objects. It accepts the component-specific kinds (imports, anatomy, api, variants, states, design-specifications) plus the general kinds (guidelines, useCases, accessibility, content, sections, checklist). The `kind` property on each entry sets its shape. Order matters, so tools SHOULD keep it for display.",
          "items": {
            "$ref": "#/$defs/componentDocumentBlock"
          },
          "minItems": 1
        },
        "agentDocumentBlocks": {
          "type": "array",
          "description": "Structured docs for agents (AI/LLM) only — a separate space that accepts the same block kinds as `documentBlocks`. `documentBlocks` is the default: put anything a human reader needs there. Use `agentDocumentBlocks` only for content that helps an agent build correctly but is noise for humans. For example: hard MUST/MUST-NOT rules an agent should treat as firm limits; notes that correct a mistake an agent is likely to make, such as reaching for this entity when a confusable one fits better; constraints backed by test-run evidence; and machine-checkable criteria with verification modes. A hard rule that humans also need (such as an accessibility requirement) belongs in `documentBlocks`, not here. Agent blocks add to the human docs — they never replace or duplicate them. Tools MUST NOT render these blocks in human-facing docs. Agents SHOULD read both arrays, human blocks first.",
          "items": {
            "$ref": "#/$defs/componentDocumentBlock"
          },
          "minItems": 1
        },
        "$extensions": {
          "$ref": "#/$defs/extensions"
        }
      },
      "additionalProperties": false
    },
    "foundation": {
      "type": "object",
      "description": "The principles, guidelines, scales, and token references that govern a broad design domain across the system, such as color, typography, spacing, elevation, motion, accessibility, and content. Foundations are wider than a single visual attribute. A domain may be visual (color, spacing) or cross-cutting (accessibility, content). The top level holds its identity: `identifier`, `name`, and `description`. The `metadata` object holds the shared entityMetadata fields (see metadata/metadata.schema.json). The `documentBlocks` array holds structured docs as typed entries: principles, scale, motion, guidelines, accessibility, content, and so on.",
      "required": [
        "kind",
        "identifier",
        "name"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "foundation",
          "description": "Identifies this entity as a foundation."
        },
        "identifier": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Machine-readable identifier for the foundation (e.g., 'color', 'typography', 'spacing', 'elevation', 'motion', 'shape', 'accessibility', 'content'). MUST be unique within its entity group."
        },
        "name": {
          "type": "string",
          "description": "Display name shown in docs (e.g., 'Color', 'Typography', 'Spacing', 'Elevation', 'Accessibility', 'Content')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this foundation covers, the design domain it governs, and its role in the system. CommonMark supported."
        },
        "metadata": {
          "$ref": "#/$defs/entityMetadata",
          "description": "Optional metadata: the shared entityMetadata fields (see metadata/metadata.schema.json)."
        },
        "relationships": {
          "$ref": "#/$defs/relationships",
          "description": "Typed dependency edges from this foundation to other entities — e.g. 'depends-on' the tokens it builds on. Inverse edges are derived by tooling. Use the `links` metadata field for external resources."
        },
        "documentBlocks": {
          "type": "array",
          "description": "All structured docs for this foundation, as an ordered array of typed document block objects. It accepts the foundation-specific kinds (principles, scale, motion) plus the general kinds (guidelines, useCases, accessibility, content, sections, checklist). The `kind` property on each entry sets its shape. Order matters, so tools SHOULD keep it for display. Put internal artifact links (such as token group references) in the `links` metadata field, not in document block entries.",
          "items": {
            "$ref": "#/$defs/foundationDocumentBlock"
          },
          "minItems": 1
        },
        "agentDocumentBlocks": {
          "type": "array",
          "description": "Structured docs for agents (AI/LLM) only — a separate space that accepts the same block kinds as `documentBlocks`. `documentBlocks` is the default: put anything a human reader needs there. Use `agentDocumentBlocks` only for content that helps an agent build correctly but is noise for humans. For example: hard MUST/MUST-NOT rules an agent should treat as firm limits; notes that correct a mistake an agent is likely to make, such as reaching for this entity when a confusable one fits better; constraints backed by test-run evidence; and machine-checkable criteria with verification modes. A hard rule that humans also need (such as an accessibility requirement) belongs in `documentBlocks`, not here. Agent blocks add to the human docs — they never replace or duplicate them. Tools MUST NOT render these blocks in human-facing docs. Agents SHOULD read both arrays, human blocks first.",
          "items": {
            "$ref": "#/$defs/foundationDocumentBlock"
          },
          "minItems": 1
        },
        "$extensions": {
          "$ref": "#/$defs/extensions"
        }
      },
      "additionalProperties": false
    },
    "guide": {
      "type": "object",
      "description": "A long-form, reading-oriented document: a getting-started walkthrough, a contribution guide, a tutorial, a conceptual overview, a migration guide, or any narrative guidance the system needs. Guides fill the gap left by the artifact-bound entities (component, token, foundation, pattern). Those entities document a single thing, not a journey or a concept. The top level holds its identity: `identifier`, `name`, and `description`. The `metadata` object holds optional fields. Use `category` (e.g., 'getting-started', 'contribution', 'tutorial', 'concept', 'migration') to classify the guide, and `tags` for finer-grained discovery. The `documentBlocks` array holds structured docs as typed entries: sections (narrative prose), steps (procedures), guidelines, useCases, imports, accessibility, and content.",
      "required": [
        "kind",
        "identifier",
        "name"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "guide",
          "description": "Identifies this entity as a guide."
        },
        "identifier": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Machine-readable identifier for the guide (e.g., 'getting-started', 'contributing', 'theming-tutorial', 'migrating-to-v2'). MUST be lowercase kebab-case and unique within its entity group."
        },
        "name": {
          "type": "string",
          "description": "Display name shown in docs (e.g., 'Getting started', 'Contributing', 'Theming tutorial', 'Migrating to v2')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this guide covers, who it is for, and what the reader will accomplish. CommonMark supported."
        },
        "metadata": {
          "$ref": "#/$defs/entityMetadata",
          "description": "Optional metadata: the shared entityMetadata fields (see metadata/metadata.schema.json). Use `category` to classify the guide (e.g., 'getting-started', 'contribution', 'tutorial', 'concept', 'migration')."
        },
        "relationships": {
          "$ref": "#/$defs/relationships",
          "description": "Typed dependency edges from this guide to the entities it documents or depends on. Inverse edges are derived by tooling. Use the `links` metadata field for external resources."
        },
        "documentBlocks": {
          "type": "array",
          "description": "All structured docs for this guide, as an ordered array of typed document block objects. It accepts the guide-specific kind (steps), the reused imports kind (setup instructions), plus the general kinds (guidelines, useCases, accessibility, content, sections, checklist). The free-form sections block is a guide's backbone. The `kind` property on each entry sets its shape. A guide reads top to bottom, so order matters and tools SHOULD keep it for display. Put links to other entities and external resources in the `links` metadata field or a section's `links`, not in document block entries.",
          "items": {
            "$ref": "#/$defs/guideDocumentBlock"
          },
          "minItems": 1
        },
        "agentDocumentBlocks": {
          "type": "array",
          "description": "Structured docs for agents (AI/LLM) only — a separate space that accepts the same block kinds as `documentBlocks`. `documentBlocks` is the default: put anything a human reader needs there. Use `agentDocumentBlocks` only for content that helps an agent build correctly but is noise for humans. For example: hard MUST/MUST-NOT rules an agent should treat as firm limits; notes that correct a mistake an agent is likely to make, such as reaching for this entity when a confusable one fits better; constraints backed by test-run evidence; and machine-checkable criteria with verification modes. A hard rule that humans also need (such as an accessibility requirement) belongs in `documentBlocks`, not here. Agent blocks add to the human docs — they never replace or duplicate them. Tools MUST NOT render these blocks in human-facing docs. Agents SHOULD read both arrays, human blocks first.",
          "items": {
            "$ref": "#/$defs/guideDocumentBlock"
          },
          "minItems": 1
        },
        "$extensions": {
          "$ref": "#/$defs/extensions"
        }
      },
      "additionalProperties": false
    },
    "pattern": {
      "type": "object",
      "description": "A repeated, multi-component solution to a common UX problem. Patterns sit above individual components. They show how those components combine to meet a user need. The top level holds its identity: `identifier`, `name`, and `description`. The `metadata` object holds the shared entityMetadata fields (see metadata/metadata.schema.json). The `documentBlock` array holds structured docs as typed entries: `interactions` for flow steps, `guidelines` for rules, `accessibility` for a11y specs, and so on.",
      "required": [
        "kind",
        "identifier",
        "name"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "pattern",
          "description": "Identifies this entity as a pattern."
        },
        "identifier": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Machine-readable identifier for the pattern (e.g., 'error-messaging', 'navigation', 'empty-state', 'form-validation'). MUST be unique within its entity group."
        },
        "name": {
          "type": "string",
          "description": "Display name shown in docs (e.g., 'Error Messaging', 'Navigation', 'Empty State')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this pattern is, the UX problem it solves, and how its components compose to meet a user need. CommonMark supported."
        },
        "metadata": {
          "$ref": "#/$defs/entityMetadata",
          "description": "Optional metadata: the shared entityMetadata fields (see metadata/metadata.schema.json)."
        },
        "relationships": {
          "$ref": "#/$defs/relationships",
          "description": "Typed dependency edges from this pattern to other entities — chiefly 'composes' the components it is built from (mark `required` true for essential ones), plus 'depends-on' and 'alternative-to'. The structural counterpart to listing components in `links`. Inverse edges are derived by tooling."
        },
        "documentBlocks": {
          "type": "array",
          "description": "All structured docs for this pattern, as an ordered array of typed document block objects. Patterns accept the pattern-specific kind (interactions), the shared structural kinds (anatomy, variants, states), plus the general kinds (guidelines, useCases, accessibility, content, sections, checklist). The `kind` property on each entry sets its shape. Order matters, so tools SHOULD keep it for display. Interaction entries rely on order to show the flow over time. Declare the components a pattern uses as typed `composes` edges in the entity's `relationships` array (with `role` describing what each component does), not in a dedicated block kind.",
          "items": {
            "$ref": "#/$defs/patternDocumentBlock"
          },
          "minItems": 1
        },
        "agentDocumentBlocks": {
          "type": "array",
          "description": "Structured docs for agents (AI/LLM) only — a separate space that accepts the same block kinds as `documentBlocks`. `documentBlocks` is the default: put anything a human reader needs there. Use `agentDocumentBlocks` only for content that helps an agent build correctly but is noise for humans. For example: hard MUST/MUST-NOT rules an agent should treat as firm limits; notes that correct a mistake an agent is likely to make, such as reaching for this entity when a confusable one fits better; constraints backed by test-run evidence; and machine-checkable criteria with verification modes. A hard rule that humans also need (such as an accessibility requirement) belongs in `documentBlocks`, not here. Agent blocks add to the human docs — they never replace or duplicate them. Tools MUST NOT render these blocks in human-facing docs. Agents SHOULD read both arrays, human blocks first.",
          "items": {
            "$ref": "#/$defs/patternDocumentBlock"
          },
          "minItems": 1
        },
        "$extensions": {
          "$ref": "#/$defs/extensions"
        }
      },
      "additionalProperties": false
    },
    "tokenOverride": {
      "type": "object",
      "description": "A single token override inside a theme: which token changes, and why. The override value lives in the DTCG file. This entry records the intent and reason.",
      "required": [
        "token"
      ],
      "properties": {
        "token": {
          "type": "string",
          "description": "The identifier of the token being overridden. MUST match the `identifier` of a token defined elsewhere in the system. Token identifiers follow no fixed pattern, so they can support DTCG and design tool naming styles (e.g., 'color-text-primary', 'color.text.primary', 'color/text/primary')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "Says what changes for this token in this theme and why (e.g., 'Lightened to meet WCAG AAA contrast on the dark background'). The resolved value lives in the DTCG file."
        }
      },
      "additionalProperties": false
    },
    "theme": {
      "type": "object",
      "description": "A named context that adapts the design system's token values. DSDS captures the purpose, usage document blocks, accessibility notes, and the list of affected tokens. The override values live in the DTCG file that `source` points to. Use themes for color modes (light, dark, high-contrast), density settings (compact, comfortable), brand variants, or product-specific tweaks. The token catalog stays the source of truth for token identity, type, and docs. The `metadata` object holds the shared entityMetadata fields (see metadata/metadata.schema.json).",
      "required": [
        "kind",
        "identifier",
        "name"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "theme",
          "description": "Identifies this entity as a theme."
        },
        "identifier": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Machine-readable identifier for the theme (e.g., 'dark', 'high-contrast', 'compact', 'brand-sub-brand')."
        },
        "name": {
          "type": "string",
          "description": "Display name for the theme (e.g., 'Dark Mode', 'High Contrast', 'Compact Density')."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this theme is for, the context it adapts the system to, and when to apply it. CommonMark supported."
        },
        "metadata": {
          "$ref": "#/$defs/entityMetadata",
          "description": "Optional metadata: the shared entityMetadata fields (see metadata/metadata.schema.json)."
        },
        "source": {
          "type": "object",
          "description": "Points to the W3C Design Tokens (DTCG) definition. DSDS does not copy token values. The DTCG file is the source of truth for resolved values, aliases, and type info.",
          "required": [
            "file"
          ],
          "properties": {
            "file": {
              "type": "string",
              "description": "URI or relative path to the DTCG file containing this theme's token override definitions."
            },
            "path": {
              "type": "string",
              "description": "The dot-path to this theme's group within the DTCG file, if applicable."
            }
          },
          "additionalProperties": false
        },
        "overrides": {
          "type": "array",
          "description": "Token overrides this theme applies. Each entry says which token changes and why. List only tokens whose values differ from the default. Unlisted tokens keep their default values. The override values live in the DTCG file that `source` points to.",
          "items": {
            "$ref": "#/$defs/tokenOverride"
          },
          "minItems": 1
        },
        "relationships": {
          "$ref": "#/$defs/relationships",
          "description": "Typed dependency edges from this theme to other entities — e.g. 'depends-on' the tokens it overrides, 'extends' a base theme. Inverse edges are derived by tooling."
        },
        "documentBlocks": {
          "type": "array",
          "description": "All structured docs for this theme, as an ordered array of typed document block objects. Themes accept the general kinds only (guidelines, useCases, accessibility, content, sections, checklist). The `kind` property on each entry sets its shape. Order matters, so tools SHOULD keep it for display.",
          "items": {
            "$ref": "#/$defs/generalDocumentBlock"
          },
          "minItems": 1
        },
        "agentDocumentBlocks": {
          "type": "array",
          "description": "Structured docs for agents (AI/LLM) only — a separate space that accepts the same block kinds as `documentBlocks`. `documentBlocks` is the default: put anything a human reader needs there. Use `agentDocumentBlocks` only for content that helps an agent build correctly but is noise for humans. For example: hard MUST/MUST-NOT rules an agent should treat as firm limits; notes that correct a mistake an agent is likely to make, such as reaching for this entity when a confusable one fits better; constraints backed by test-run evidence; and machine-checkable criteria with verification modes. A hard rule that humans also need (such as an accessibility requirement) belongs in `documentBlocks`, not here. Agent blocks add to the human docs — they never replace or duplicate them. Tools MUST NOT render these blocks in human-facing docs. Agents SHOULD read both arrays, human blocks first.",
          "items": {
            "$ref": "#/$defs/generalDocumentBlock"
          },
          "minItems": 1
        },
        "$extensions": {
          "$ref": "#/$defs/extensions"
        }
      },
      "additionalProperties": false
    },
    "token": {
      "type": "object",
      "description": "Docs for a single design token: identifier, type, use cases, guidelines, and accessibility notes. The display `name` property is absent. The `identifier` itself serves as the display label. Description and status are optional. The `metadata` object holds the shared entityMetadata fields (see metadata/metadata.schema.json). When a token sits inside a token group and omits its own status field, the parent group's status applies.",
      "required": [
        "kind",
        "identifier"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "token",
          "description": "Identifies this entity as a token."
        },
        "identifier": {
          "type": "string",
          "description": "The token identifier. It follows no fixed pattern, so it can support DTCG and design tool naming styles that use dots, slashes, or other separators (e.g., 'color-text-primary', 'color.text.primary', 'color/text/primary'). It also serves as the human-readable display label."
        },
        "name": {
          "type": "string",
          "description": "Optional display name (e.g., 'Primary Text'). When omitted, the `identifier` serves as the display label — the uniform rule for all entities is: display `name` when present, otherwise `identifier`."
        },
        "tokenType": {
          "type": "string",
          "description": "The type of token aligned with the DTCG spec (e.g., 'color', 'dimension', 'fontFamily', 'fontWeight', 'duration', 'cubicBezier', 'shadow'). MUST be present unless an ancestor token group declares `tokenType` — children inherit the group's value when they omit their own. Validators enforce the own-or-inherited rule; a token with no `tokenType` anywhere in its ancestry is a defect."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this token represents and when to use it. Optional, so keep it terse at scale. CommonMark supported."
        },
        "source": {
          "type": "object",
          "description": "Points to the W3C Design Tokens (DTCG) definition. DSDS does not copy token values. The DTCG file is the source of truth for resolved values, aliases, and type info.",
          "required": [
            "file"
          ],
          "properties": {
            "file": {
              "type": "string",
              "description": "URI or relative path to the DTCG file containing this token's definition."
            },
            "path": {
              "type": "string",
              "description": "The dot-path to this token within the DTCG file (e.g., 'color.text.primary')."
            }
          },
          "additionalProperties": false
        },
        "metadata": {
          "$ref": "#/$defs/entityMetadata",
          "description": "Optional metadata: the shared entityMetadata fields (see metadata/metadata.schema.json)."
        },
        "relationships": {
          "$ref": "#/$defs/relationships",
          "description": "Typed dependency edges to other entities — e.g. 'depends-on' a token this one aliases, 'part-of' a token group. Inverse edges are derived by tooling."
        },
        "documentBlocks": {
          "type": "array",
          "description": "All structured docs for this token, as an ordered array of typed document block objects. Tokens accept the general kinds only (guidelines, useCases, accessibility, content, sections, checklist). The `kind` property on each entry sets its shape. Order matters, so tools SHOULD keep it for display.",
          "items": {
            "$ref": "#/$defs/generalDocumentBlock"
          },
          "minItems": 1
        },
        "agentDocumentBlocks": {
          "type": "array",
          "description": "Structured docs for agents (AI/LLM) only — a separate space that accepts the same block kinds as `documentBlocks`. `documentBlocks` is the default: put anything a human reader needs there. Use `agentDocumentBlocks` only for content that helps an agent build correctly but is noise for humans. For example: hard MUST/MUST-NOT rules an agent should treat as firm limits; notes that correct a mistake an agent is likely to make, such as reaching for this entity when a confusable one fits better; constraints backed by test-run evidence; and machine-checkable criteria with verification modes. A hard rule that humans also need (such as an accessibility requirement) belongs in `documentBlocks`, not here. Agent blocks add to the human docs — they never replace or duplicate them. Tools MUST NOT render these blocks in human-facing docs. Agents SHOULD read both arrays, human blocks first.",
          "items": {
            "$ref": "#/$defs/generalDocumentBlock"
          },
          "minItems": 1
        },
        "$extensions": {
          "$ref": "#/$defs/extensions"
        }
      },
      "additionalProperties": false
    },
    "tokenGroup": {
      "type": "object",
      "description": "A group of related design tokens. Token groups are flexible units: a full collection, a family (e.g., all color tokens), a sub-family (e.g., a single hue with its grade scale), or any other logical grouping. Groups can nest to form a deep hierarchy. A top-level group might stand for 'color' and hold child groups for 'color-text', 'color-background', and 'color-border', each with its own tokens. This nesting mirrors how real design systems organize tokens into families and sub-families. The display `name` property is absent. The `identifier` itself serves as the display label. Description and status are optional. The `metadata` object holds the shared entityMetadata fields (see metadata/metadata.schema.json). When a child omits its own status field, the parent group's status applies.",
      "required": [
        "kind",
        "identifier"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "token-group",
          "description": "Identifies this entity as a token-group."
        },
        "identifier": {
          "type": "string",
          "description": "The token group identifier. It follows no fixed pattern, so it can support DTCG and design tool naming styles (e.g., 'color-text', 'color.text', 'color/text'). It also serves as the human-readable display label."
        },
        "name": {
          "type": "string",
          "description": "Optional display name (e.g., 'Primary Text'). When omitted, the `identifier` serves as the display label — the uniform rule for all entities is: display `name` when present, otherwise `identifier`."
        },
        "description": {
          "$ref": "#/$defs/richText",
          "description": "What this token group covers and how it is organized. Optional, so keep it terse at scale. CommonMark supported."
        },
        "tokenType": {
          "type": "string",
          "description": "When all tokens in this group share the same DTCG token type, declare it here so you don't repeat it on each child. Each token in children MAY override this value. Common values: 'color', 'dimension', 'fontFamily', 'fontWeight', 'duration', 'number', 'shadow'."
        },
        "source": {
          "type": "object",
          "description": "Points to the W3C Design Tokens (DTCG) definition. DSDS does not copy token values. The DTCG file is the source of truth for resolved values, aliases, and type info.",
          "required": [
            "file"
          ],
          "properties": {
            "file": {
              "type": "string",
              "description": "URI or relative path to the DTCG file containing this group's definition."
            },
            "path": {
              "type": "string",
              "description": "The dot-path to this group within the DTCG file (e.g., 'color.text')."
            }
          },
          "additionalProperties": false
        },
        "children": {
          "type": "array",
          "description": "An ordered array of tokens and/or nested token groups inside this group. Each item MUST be either a token or a tokenGroup. Order often shows a meaningful progression (e.g., lightest to darkest, or smallest to largest), so it matters and tools SHOULD keep it for display. Children inherit tokenType and status from this group when they omit their own values.",
          "items": {
            "oneOf": [
              {
                "$ref": "#/$defs/token"
              },
              {
                "$ref": "#/$defs/tokenGroup"
              }
            ]
          },
          "minItems": 1
        },
        "metadata": {
          "$ref": "#/$defs/entityMetadata",
          "description": "Optional metadata: the shared entityMetadata fields (see metadata/metadata.schema.json)."
        },
        "relationships": {
          "$ref": "#/$defs/relationships",
          "description": "Typed dependency edges to other entities — e.g. 'depends-on' a token this one aliases, 'part-of' a token group. Inverse edges are derived by tooling."
        },
        "documentBlocks": {
          "type": "array",
          "description": "All structured docs for this token group, as an ordered array of typed document block objects. It accepts the general kinds only (guidelines, useCases, accessibility, content, sections, checklist). Use this array for docs that apply to the group as a whole. Put docs for individual tokens on the token inside `children`.",
          "items": {
            "$ref": "#/$defs/generalDocumentBlock"
          },
          "minItems": 1
        },
        "agentDocumentBlocks": {
          "type": "array",
          "description": "Structured docs for agents (AI/LLM) only — a separate space that accepts the same block kinds as `documentBlocks`. `documentBlocks` is the default: put anything a human reader needs there. Use `agentDocumentBlocks` only for content that helps an agent build correctly but is noise for humans. For example: hard MUST/MUST-NOT rules an agent should treat as firm limits; notes that correct a mistake an agent is likely to make, such as reaching for this entity when a confusable one fits better; constraints backed by test-run evidence; and machine-checkable criteria with verification modes. A hard rule that humans also need (such as an accessibility requirement) belongs in `documentBlocks`, not here. Agent blocks add to the human docs — they never replace or duplicate them. Tools MUST NOT render these blocks in human-facing docs. Agents SHOULD read both arrays, human blocks first.",
          "items": {
            "$ref": "#/$defs/generalDocumentBlock"
          },
          "minItems": 1
        },
        "$extensions": {
          "$ref": "#/$defs/extensions"
        }
      },
      "additionalProperties": false
    },
    "anyEntity": {
      "type": "object",
      "description": "Any DSDS entity — component, token, token group, theme, foundation, pattern, guide, or chunk. The required `kind` property identifies the entity type and selects its schema via one `if`/`then` branch per kind — the same discriminated-union style the document-block unions use, so a validation error reports against the one schema the `kind` selects instead of compounding across all eight. This is the single entity union. The root `entity` property and each entity group's `entities` array both accept it.",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "enum": [
            "component",
            "pattern",
            "foundation",
            "theme",
            "token",
            "token-group",
            "guide",
            "chunk"
          ]
        }
      },
      "allOf": [
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "component"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/component"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "pattern"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/pattern"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "foundation"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/foundation"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "theme"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/theme"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "token"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/token"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "token-group"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/tokenGroup"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "guide"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/guide"
          }
        },
        {
          "if": {
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "const": "chunk"
              }
            }
          },
          "then": {
            "$ref": "#/$defs/chunk"
          }
        }
      ]
    },
    "fileRef": {
      "type": "object",
      "description": "A JSON Reference to a DSDS entity or entity group defined in another file. Tooling (e.g. json-schema-ref-parser) resolves the reference before validation and merge. The URI should include a JSON Pointer fragment to target a specific value within the referenced file (e.g. './button.dsds.json#/entity', './groups/tokens.dsds.json#/entityGroups/0').",
      "required": [
        "$ref"
      ],
      "properties": {
        "$ref": {
          "type": "string",
          "pattern": "^(?![a-zA-Z][a-zA-Z0-9+.\\-]*:)(?!//)(?!/)",
          "description": "A relative reference to a DSDS file, optionally with a JSON Pointer fragment that identifies the target value within that file (e.g., './button.dsds.json#/entity'). MUST be relative — the pattern rejects absolute paths, protocol-relative references, and scheme-bearing URIs (http:, file:, …). A reference to another system uses `extends.url`. Resolvers MUST detect cycles. They MUST treat unresolvable files or pointers and shape mismatches as fatal. They MUST gate any remote dereferencing behind an allow-list (see the spec's reference-resolution and security rules).",
          "minLength": 1
        }
      },
      "additionalProperties": false
    },
    "entityGroup": {
      "type": "object",
      "description": "A named collection of design system entities. All entities live in a single `entities` array, in display order, mixing entity types freely. Each entity's required `kind` identifies its type, so no per-type arrays are needed. A group MUST hold at least one entity — an empty group has nothing to document.",
      "required": [
        "name",
        "entities"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable name of the collection (e.g., 'Acme Design System', 'Color Tokens', 'Button Documentation'). This is already a display label — it does NOT have an `identifier` counterpart and need not match a slug pattern.",
          "minLength": 1
        },
        "description": {
          "$ref": "#/$defs/richText"
        },
        "entities": {
          "type": "array",
          "description": "The entities in this group, in display order. Any mix of entity kinds — components, tokens, token groups, themes, foundations, patterns, guides, and chunks. Each item's `kind` identifies its type. Items may be inlined or referenced through a $ref object that points to another DSDS file. Order is significant: tools SHOULD keep it for display.",
          "items": {
            "oneOf": [
              {
                "$ref": "#/$defs/anyEntity"
              },
              {
                "$ref": "#/$defs/fileRef"
              }
            ]
          },
          "minItems": 1
        },
        "$extensions": {
          "$ref": "#/$defs/extensions"
        }
      },
      "additionalProperties": false
    }
  }
}
