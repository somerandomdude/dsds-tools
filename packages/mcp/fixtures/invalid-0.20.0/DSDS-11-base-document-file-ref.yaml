# expect: DSDS-11
# rejectedBy: semantic
# The base document's OWN top-level `refs` - the multi-file split the
# quickstart recommends for a large system. Distinct from
# DSDS-11-file-ref-exists.yaml, which pins the same rule on an entry's
# sourceFiles: these two travel through different code paths, and the
# document-level one went unchecked entirely until this fixture existed.
schemaVersion: "0.20.0"
name: Base Document With A Dead File Ref
entries:
  - id: button
    kind: component
    name: Button
    description: An entry that exists, in a document whose own refs point at one that doesn't.
refs:
  - href: ./components/this-file-does-not-exist.dsds.yaml
    rel: file
    role: Missing component
