# expect: DSDS-11
# rejectedBy: semantic
id: bad-file-ref
kind: component
name: Bad File Ref
description: A component whose sourceFiles entry points at a file that doesn't exist on disk.
sourceFiles:
  - platform: react
    file: ./this-file-does-not-exist.tsx
