// Frontend entry point. Registering the app custom element (which imports the
// column components) is all that's needed — the <dsds-app> tag in index.html
// upgrades and boots the application.

import './components/dsds-app.js';
