{
  "id": "tag-import",
  "stratum": "supported",
  "task": "Extract the documented import and custom-element tag for Tag.",
  "evidence": [["get", "tag", "--block", "imports"]],
  "prompt": "Return JSON only with evidence_quote, import, and tag. Quote the import description unchanged and return the documented import statement and custom-element tag.",
  "expect": {
    "requiredFields": ["evidence_quote", "import", "tag"],
    "fields": {
      "evidence_quote": { "requiredLiterals": ["Registers <ds-tag> only"] },
      "import": { "equals": "import \"dsds-components/tag\";" },
      "tag": { "equals": "<ds-tag>" }
    }
  }
}
