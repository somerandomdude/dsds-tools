{
  "id": "table-import",
  "stratum": "supported",
  "task": "Extract the documented import and custom-element tag for Table.",
  "evidence": [["get", "table", "--block", "imports"]],
  "prompt": "Return JSON only with evidence_quote, import, and tag. Quote the import description unchanged and return the documented import statement and custom-element tag.",
  "expect": {
    "requiredFields": ["evidence_quote", "import", "tag"],
    "fields": {
      "evidence_quote": { "requiredLiterals": ["Registers <ds-table> only"] },
      "import": { "equals": "import \"dsds-components/table\";" },
      "tag": { "equals": "<ds-table>" }
    }
  }
}
