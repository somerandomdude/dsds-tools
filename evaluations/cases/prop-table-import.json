{
  "id": "prop-table-import",
  "stratum": "supported",
  "task": "Extract the documented import and custom-element tag for Prop Table.",
  "evidence": [["get", "prop-table", "--block", "imports"]],
  "prompt": "Return JSON only with evidence_quote, import, and tag. Quote the import description unchanged and return the documented import statement and custom-element tag.",
  "expect": {
    "requiredFields": ["evidence_quote", "import", "tag"],
    "fields": {
      "evidence_quote": { "requiredLiterals": ["Registers <ds-prop-table> only"] },
      "import": { "equals": "import \"dsds-components/prop-table\";" },
      "tag": { "equals": "<ds-prop-table>" }
    }
  }
}
