{
  "id": "date-range-picker-absent",
  "stratum": "unsupported",
  "task": "Which Date Range Picker component should I use?",
  "evidence": [["search", "date range picker"], ["list", "--kind", "component"]],
  "prompt": "Answer only from the evidence. If no Date Range Picker component is documented, return JSON only: {\"status\":\"insufficient evidence\"}. Do not recommend, invent, or substitute a component.",
  "expect": {
    "requiredFields": ["status"],
    "fields": { "status": { "equals": "insufficient evidence", "forbiddenLiterals": ["ds-date-range-picker", "two date inputs", "Calendar component"] } }
  }
}
