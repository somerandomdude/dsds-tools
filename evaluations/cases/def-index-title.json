{
  "id": "def-index-title",
  "stratum": "supported",
  "task": "Extract Definition Index's documented title property.",
  "evidence": [["get", "def-index", "--block", "api"]],
  "prompt": "Return JSON only with evidence_quote, property, and behavior. Every value must be a string. Quote the title-property description unchanged and copy its identifier and behavior.",
  "expect": {
    "requiredFields": ["evidence_quote", "property", "behavior"],
    "fields": {
      "evidence_quote": { "equals": "Title bar shown above the index. Omit for no title." },
      "property": { "equals": "title" },
      "behavior": { "requiredLiterals": ["title", "above the index", "omit"] }
    }
  }
}
